# 路云天瞳 — 最新项目开发规划文档

> **文档版本**：v2.0 | **生成日期**：2026-07-08
> **基于**：实际代码审查 + 《路云天瞳-完整项目文档.md》+《路云天瞳-前端优化任务书.md》
> **核心目标**：答辩前完成全部 P0 修复 + 前端视觉升级 + 全屏演示模式，达到"大厂级产品视觉与交互品质"

---

## 一、当前项目现状总览

### 1.1 里程碑状态

| 里程碑 | 描述 | 状态 | 完成度 |
|--------|------|------|--------|
| M0 | 端到端 Demo 闭环（无硬件/MQTT/YOLO/PyTorch） | ✅ 完成 | 100% |
| M1 | 仿真路端+车端 MQTT 协同 | 🔄 进行中 | 85% |
| M2 | DAIR-V2X 数据集 + 模型评估 | ⚠️ 部分完成 | 60% |
| M3 | 硬件部署（Jetson/智能车） | ❌ 未开始 | 0% |

### 1.2 代码规模

| 维度 | 数量 | 状态 |
|------|------|------|
| 后端 Python 文件 | 70+ | 功能完整，3 个紧急 Bug |
| 前端 TS/TSX 文件 | 83 | 功能完整，视觉待升级 |
| 后端测试文件 | 18 | 覆盖面广 |
| 前端测试文件 | 4 | 仅纯逻辑测试，无组件测试 |
| 文档文件 | 12+ (.md) | 部分过时 |
| 脚本文件 | 22 | Windows/Linux 全覆盖 |
| Docker 服务 | 6 | 定义完整，未实际验证 |
| CI 流水线 | 2 | backend + algorithm |

### 1.3 后端待修复项（代码审查确认）

| 编号 | 严重度 | 文件 | 行号 | 问题 | 当前代码 | 修复方案 |
|------|--------|------|------|------|----------|----------|
| B1 | 🔴 紧急 | `src/cloud_twin/data_store.py` | 182-186 | SQL 注入 | COUNT 查询用 f-string 插值 `scene_id`/`severity` | 改用参数化 `?` 占位符 |
| B2 | 🔴 紧急 | `src/cloud_twin/api.py` | 48-54 | CORS+credentials 冲突 | `allow_origins=["http://localhost:5173", "http://localhost:3000", "*"]` + `allow_credentials=True`，浏览器会拒绝 | 移除 `"*"`，保留具体 origins |
| B3 | 🔴 紧急 | `src/cloud_twin/api.py` | 103-104, 132-133 | 404 响应格式错误 | `return {"error": "..."}, 404` 返回元组，FastAPI 不生成正确 HTTP 错误响应 | 改为 `raise HTTPException(status_code=404, detail="...")` |

### 1.4 前端待修复项（代码审查确认）

| 编号 | 严重度 | 文件 | 行号 | 问题 | 当前代码 | 修复方案 |
|------|--------|------|------|------|----------|----------|
| F1 | 🟡 中 | `ConnectionPanel.tsx` | 43 | "重新连接"按钮永久 disabled，无 onClick | `<Button icon={<ReloadOutlined />} disabled={!connection.connected}>重新连接</Button>` | 移除 disabled，添加 `onClick={() => wsService.connect()}` |
| F2 | 🟡 中 | `monitorStore.ts` | 74-77 | `toggleConnection` 仅切换布尔值，不控制 WebSocket | `set((state) => ({ connection: { ...state.connection, connected: !state.connection.connected } }))` | 调用 `wsService.connect()` / `wsService.disconnect()` |
| F3 | 🟢 低 | `EvaluationPage.tsx` | 232 | 导出按钮是 placeholder | `onClick={() => message.info('导出功能将在接入后端后启用')}` | 调用 `downloadJson({metrics, baselines, ablations, targetStatus}, 'evaluation-report.json')` |

### 1.5 前端视觉待升级项（代码审查确认）

| 编号 | 当前状态 | 升级目标 |
|------|----------|----------|
| V1 | 3D 场景：基础 Box/Capsule 几何体，无后处理、无粒子、无建筑 | Bloom 后处理 + 数据流粒子 + 赛博朋克建筑 + 传感器锥体 + 交通灯 |
| V2 | KpiBar：4 个 Ant Design Card + 图标 + 数字，无 sparkline、无趋势箭头 | Sparkline + 趋势箭头 + Orbitron 字体 + 错开入场动画 |
| V3 | RiskList：标准 Ant Design Table | 自定义列表 + 风险竖线 + 脉冲背景 + 排名数字 |
| V4 | LogStream：基础 Tag + 文字，无终端风格 | 扫描线动画 + 四角L型装饰 + 级别色标 + 终端风格 |
| V5 | ECharts：使用内置 `dark` 主题，无自定义 | 注册 `v2x-dark` 自定义主题 + 渐变面积 + 发光效果 |
| V6 | MainLayout：标准 Ant Design Layout，Logo 仅文字 | SVG 图标 + Orbitron 品牌字 + 底部状态区 + 选中竖线 |
| V7 | 无设计系统：颜色硬编码、间距不统一、无排版层级 | design-tokens.ts + 升级 colors.ts + 升级 global.css |
| V8 | 全部使用 inline style | CSS Modules + 全局工具类 |
| V9 | 无全屏演示模式 | 新建 `/presentation` 路由，65/35 布局 |
| V10 | 无通用组件（Toast/EmptyState/Skeleton） | 新建 3 个通用组件 |

### 1.6 文档待修复项

| 编号 | 文件 | 问题 | 修复方案 |
|------|------|------|----------|
| D1 | `docs/ARCHITECTURE.md` | 引用 InfluxDB，实际用 SQLite | 全文替换 InfluxDB → SQLite WAL |
| D2 | `frontend/README.md` | 描述"纯前端 UI demo + mock 数据"，实际已接真实后端 | 更新为当前状态描述 |
| D3 | `docs/MVP_REQUIREMENTS.md` | DoD 复选框未更新 | 根据 GOAL.md 更新勾选状态 |
| D4 | 根目录 | 无 README.md | 新建项目级 README |
| D5 | `docs/WEEKLY_PLAN.md` | 7 天计划，实际远超 | 标记为 historical |

### 1.7 代码清理项

| 编号 | 文件 | 清理项 | 当前状态 |
|------|------|--------|----------|
| C1 | `types/cloud.ts` | `CloudFusionResult`、`CloudAlert` 未使用 | 定义但无引用 |
| C2 | `types/common.ts` | `KpiItem`、`BBox` 未使用 | 定义但无引用 |
| C3 | `shared/utils/format.ts` | `formatDate`、`formatPercent`、`formatLatency`、`riskLevelToNumber`、`numberToRiskLevel` 未使用 | 5 个函数无引用（`formatTimestamp` 已使用） |
| C4 | `shared/utils/helpers.ts` | `clamp`、`lerp` 未使用 | 2 个函数无引用 |
| C5 | `constants/config.ts` | `NAV_ITEMS` 未使用 | MainLayout 自定义 menuItems |
| C6 | `constants/config.ts` | `MQTT_DEFAULT_BROKER` 未使用 | monitorStore 硬编码 `ws://localhost:9001` |
| C7 | `ConnectionPanel.tsx` | UI 显示 "MQTT 连接面板" | 实际用 WebSocket 协议，应改为 "WebSocket 连接面板" |
| C8 | `tsconfig.json` | `noUnusedLocals: false`、`noUnusedParameters: false` | 应设为 `true` 强制清理 |

---

## 二、开发排期总览

### 2.1 五日冲刺计划

| 日期 | 主题 | 核心交付 | 验收点 |
|------|------|----------|--------|
| **Day 0** | 紧急修复 | 后端 3 Bug + 前端 3 Bug | `pytest` 全通过 + `npm run build` 无错误 |
| **Day 1** | 设计系统 + 布局 | design-tokens.ts + colors 升级 + global.css 升级 + MainLayout 升级 + PageHeader | 无硬编码颜色/间距 |
| **Day 2** | 组件升级 | KpiBar + RiskList + LogStream + ECharts 主题 + 图表升级 | 4 个核心组件视觉达标 |
| **Day 3** | 3D 场景升级 | Bloom + 粒子 + 建筑 + 交通灯 + 传感器锥体 + 遮挡盲区 | 3D 场景 ≥30fps + 视觉"爆炸好看" |
| **Day 4** | 页面升级 + 全屏模式 | 5 页面升级 + PresentationPage + 通用组件 | 逐页检查通过 |
| **Day 5** | 代码清理 + 测试 + 验证 | 死代码清理 + inline style 消除 + 组件测试 + 构建优化 | 全量验证通过 |

### 2.2 任务优先级矩阵

```
紧急度 ↑
│
│  P0 (Day 0)                    P1 (Day 1-3)
│  ┌─────────────────┐          ┌─────────────────────┐
│  │ 后端 3 Bug 修复  │          │ 设计系统重构          │
│  │ 前端 3 Bug 修复  │          │ 组件升级 (KpiBar等)   │
│  └─────────────────┘          │ 3D 场景 Bloom+粒子    │
│                               └─────────────────────┘
│  P2 (Day 4)                    P3 (Day 5 / 可跳过)
│  ┌─────────────────┐          ┌─────────────────────┐
│  │ 全屏演示模式      │          │ 死代码清理            │
│  │ 页面级升级        │          │ TypeScript 严格化     │
│  │ 文档修复          │          │ 组件级测试            │
│  └─────────────────┘          │ 构建优化 (gzip)       │
│                               └─────────────────────┘
└──────────────────────────────────────────────────────→ 时间
```

---

## 三、Day 0 — 紧急修复（答辩前必须完成）

### 3.1 后端 Bug 修复

#### 3.1.1 修复 SQL 注入（B1）

**文件**：`src/cloud_twin/data_store.py`
**位置**：`get_events()` 方法，约第 182-186 行
**当前代码**：
```python
total_row = conn.execute(
    "SELECT COUNT(*) FROM events WHERE 1=1" +
    (f" AND scene_id='{scene_id}'" if scene_id else "") +
    (f" AND severity='{severity}'" if severity else ""),
).fetchone()
```
**修复后**：
```python
count_sql = "SELECT COUNT(*) FROM events WHERE 1=1"
count_params = []
if scene_id:
    count_sql += " AND scene_id = ?"
    count_params.append(scene_id)
if severity:
    count_sql += " AND severity = ?"
    count_params.append(severity)
total_row = conn.execute(count_sql, count_params).fetchone()
```
**工作量**：30 分钟
**验证**：`pytest tests/test_demo_engine.py -v`

#### 3.1.2 修复 CORS 配置（B2）

**文件**：`src/cloud_twin/api.py`
**位置**：第 48-54 行
**当前代码**：
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173", "http://localhost:3000", "*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```
**修复后**：
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173", "http://localhost:3000"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```
**工作量**：15 分钟
**验证**：浏览器 DevTools Network 面板无 CORS 错误

#### 3.1.3 修复 404 响应格式（B3）

**文件**：`src/cloud_twin/api.py`
**位置**：第 103-104 行（`get_frame`）和第 132-133 行（`get_event_detail`）
**当前代码**：
```python
# get_frame
if frame is None:
    return {"error": "Frame not found"}, 404

# get_event_detail
if result is None:
    return {"error": "Event not found"}, 404
```
**修复后**：
```python
# get_frame
if frame is None:
    raise HTTPException(status_code=404, detail="Frame not found")

# get_event_detail
if result is None:
    raise HTTPException(status_code=404, detail="Event not found")
```
**工作量**：30 分钟
**验证**：`curl http://localhost:8000/api/v1/frames/99999` 返回标准 404 JSON

### 3.2 前端 Bug 修复

#### 3.2.1 修复"重新连接"按钮（F1）

**文件**：`frontend/src/widgets/connection-panel/ConnectionPanel.tsx`
**位置**：第 43 行
**当前代码**：
```tsx
<Button icon={<ReloadOutlined />} disabled={!connection.connected}>
  重新连接
</Button>
```
**修复后**：
```tsx
<Button
  icon={<ReloadOutlined />}
  onClick={() => wsService.connect()}
>
  重新连接
</Button>
```
**需要**：从 `@/services/websocketService` 导入 `wsService`
**工作量**：15 分钟

#### 3.2.2 修复 toggleConnection（F2）

**文件**：`frontend/src/store/monitorStore.ts`
**位置**：第 74-77 行
**当前代码**：
```typescript
toggleConnection: () =>
  set((state) => ({
    connection: { ...state.connection, connected: !state.connection.connected },
  })),
```
**修复后**：
```typescript
toggleConnection: () => {
  const state = get();
  if (state.connection.connected) {
    wsService.disconnect();
  } else {
    wsService.connect();
  }
  // 连接状态由 wsService 的 onConnectionChange 回调更新
},
```
**需要**：从 `@/services/websocketService` 导入 `wsService`；在 store 创建时注册 `wsService.onConnectionChange()` 回调
**工作量**：30 分钟

#### 3.2.3 修复导出按钮（F3）

**文件**：`frontend/src/pages/evaluation/EvaluationPage.tsx`
**位置**：第 232 行
**当前代码**：
```tsx
<Button
  icon={<DownloadOutlined />}
  size="small"
  onClick={() => message.info('导出功能将在接入后端后启用')}
>
  导出报告
</Button>
```
**修复后**：
```tsx
<Button
  icon={<DownloadOutlined />}
  size="small"
  onClick={() => {
    downloadJson(
      { metrics, baselines, ablations, targetStatus, summary, selectedReportKey },
      `evaluation-report-${selectedReportKey || 'default'}.json`
    );
    message.success('报告已导出');
  }}
>
  导出报告
</Button>
```
**需要**：从 `@/shared/utils/helpers` 导入 `downloadJson`（该函数已存在，见 `helpers.ts:25-33`）
**工作量**：10 分钟

### 3.3 Day 0 验收清单

- [ ] `python -m pytest tests/` 全部通过
- [ ] `cd frontend && npm run build` 无错误
- [ ] `curl http://localhost:8000/api/v1/frames/99999` 返回标准 404
- [ ] 浏览器 DevTools 无 CORS 错误
- [ ] `/monitor` 页面"重新连接"按钮可点击且功能正常
- [ ] `/monitor` 页面"断开连接"按钮实际断开 WebSocket
- [ ] `/evaluation` 页面"导出报告"按钮实际下载 JSON 文件

---

## 四、Day 1 — 设计系统重构 + 布局升级

### 4.1 新建设计系统文件

#### 4.1.1 新建 `src/constants/design-tokens.ts`

**交付物**：完整的排版/间距/圆角/阴影/动效/Z-index 常量定义

```typescript
// ═══ 排版 ═══
export const TYPOGRAPHY = {
  fontFamily: "'Inter', -apple-system, BlinkMacSystemFont, sans-serif",
  fontFamilyMono: "'JetBrains Mono', 'Fira Code', monospace",
  fontFamilyDisplay: "'Orbitron', sans-serif",
  fontSize: { xs: 10, sm: 12, base: 14, lg: 16, xl: 20, xxl: 28, display: 36 },
  fontWeight: { normal: 400, medium: 500, semibold: 600, bold: 700, black: 900 },
  lineHeight: { tight: 1.2, normal: 1.5, relaxed: 1.8 },
  letterSpacing: { tight: -0.02, normal: 0, wide: 0.05, extraWide: 0.15 },
} as const;

// ═══ 间距（8px 栅格） ═══
export const SPACING = { xs: 4, sm: 8, md: 12, base: 16, lg: 20, xl: 24, xxl: 32 } as const;

// ═══ 圆角 ═══
export const RADIUS = { sm: 4, base: 6, md: 8, lg: 12, full: 9999 } as const;

// ═══ 阴影层级 ═══
export const SHADOWS = {
  card: '0 2px 12px rgba(0, 0, 0, 0.3)',
  cardHover: '0 8px 32px rgba(0, 212, 255, 0.12)',
  popover: '0 12px 40px rgba(0, 0, 0, 0.5)',
  glowCyan: '0 0 20px rgba(0, 212, 255, 0.3)',
  glowGreen: '0 0 20px rgba(0, 255, 136, 0.3)',
  glowRed: '0 0 20px rgba(255, 77, 79, 0.3)',
} as const;

// ═══ 动效曲线 ═══
export const MOTION = {
  durationFast: '0.15s', durationBase: '0.3s', durationSlow: '0.5s',
  easeOut: 'cubic-bezier(0.16, 1, 0.3, 1)',
  easeInOut: 'cubic-bezier(0.65, 0, 0.35, 1)',
  spring: 'cubic-bezier(0.34, 1.56, 0.64, 1)',
} as const;

// ═══ Z-index 层级 ═══
export const Z_INDEX = { base: 0, dropdown: 100, sticky: 200, modal: 1000, toast: 1100, tooltip: 1200 } as const;
```

**工作量**：30 分钟

#### 4.1.2 升级 `src/constants/colors.ts`

**新增内容**：渐变色 `GRADIENTS` + 语义色 `SEMANTIC_COLORS`

```typescript
// 在现有 RISK_COLORS / CHART_COLORS / THEME_COLORS / LOG_LEVEL_COLORS 基础上添加

export const GRADIENTS = {
  bgPrimary: 'linear-gradient(135deg, #0a0e1a 0%, #0d1424 100%)',
  bgCard: 'linear-gradient(135deg, rgba(16,24,48,0.9) 0%, rgba(10,14,26,0.95) 100%)',
  dataFlow: 'linear-gradient(90deg, #00d4ff, #00ff88)',
  riskBar: 'linear-gradient(90deg, #52c41a, #faad14, #ff7a45, #ff4d4f)',
  titleShimmer: 'linear-gradient(90deg, #00d4ff, #00ff88, #00d4ff)',
} as const;

export const SEMANTIC_COLORS = {
  live: '#00ff88', mock: '#faad14',
  online: '#00ff88', offline: '#ff4d4f', connecting: '#faad14',
  low: '#52c41a', medium: '#faad14', high: '#ff7a45', critical: '#ff4d4f',
} as const;
```

**工作量**：15 分钟

#### 4.1.3 升级 `src/app/styles/global.css`

**新增 CSS 类**：

| 类名 | 用途 | 关键样式 |
|------|------|----------|
| `.glass-card` (升级) | 毛玻璃卡片 | 渐变背景 + 顶部发光线 `::before` + hover 上浮 |
| `.tech-border` | 四角L型装饰 | `::before`/`::after` 绝对定位 12px L 型边框 |
| `.data-pulse` | 数据脉冲 | `dataPulse` 2s ease-in-out infinite |
| `.scan-line` | 扫描线 | `scanLine` 3s linear infinite |
| `.skeleton` | 骨架屏 | `shimmer` 1.5s 渐变扫光 |
| `.empty-state` | 空状态 | flex 居中 + 灰色文字 |
| `.fade-in-stagger` | 错开入场 | `:nth-child(1)` ~ `:nth-child(6)` 延迟 0~0.3s |
| `.number-flip` | 数字翻转 | `numberFlip` translateY 8px→0 |

**升级 body 背景**：
```css
body {
  background: linear-gradient(135deg, #050816 0%, #0a0e1a 50%, #0d1424 100%);
  background-image:
    linear-gradient(135deg, #050816 0%, #0a0e1a 50%, #0d1424 100%),
    linear-gradient(rgba(0,212,255,0.015) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,212,255,0.015) 1px, transparent 1px);
  background-size: 100% 100%, 40px 40px, 40px 40px;
  background-attachment: fixed, fixed, fixed;
}
```

**工作量**：1 小时

#### 4.1.4 引入 Orbitron 字体

**文件**：`frontend/index.html`
**新增**：
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Orbitron:wght@400;700;900&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
```

**用途映射**：
- `Orbitron` → KPI 数字、大屏标题、科技感标签
- `Inter` → 正文
- `JetBrains Mono` → 日志、代码

**工作量**：10 分钟

### 4.2 MainLayout 升级

**文件**：`frontend/src/app/layout/MainLayout.tsx`
**当前问题**：
1. Logo 区域仅文字 "V2X 安全防御平台"，无品牌图标
2. 菜单项硬编码 `menuItems`，未使用 `config.ts` 中的 `NAV_ITEMS`
3. Header 仅一行文字 + Badge，信息密度低
4. 侧边栏无底部状态区域

**升级方案**：

```
侧边栏升级：
├── Logo 区域 (64px)
│   ├── SVG 图标（雷达/盾牌造型）
│   ├── 文字"路云天瞳"用 Orbitron 字体
│   └── 底部青色渐变线
├── 菜单项
│   ├── 选中项：左侧 3px 青色竖线 + 背景半透明青色
│   ├── 悬浮项：背景微亮 + 左侧竖线渐显
│   └── 图标和文字间距 12px
└── 底部状态区 (80px)
    ├── 系统状态指示灯（绿色脉冲圆点）
    ├── "SYSTEM ONLINE" 文字
    └── API 延迟数字

Header 升级：
├── 左侧：面包屑 + 当前页面描述
├── 右侧
│   ├── Live/Mock 数据源标签
│   ├── WebSocket 连接状态指示器
│   ├── 当前时间（Orbitron 字体）
│   └── 主题切换按钮
```

**同时修复**：让 MainLayout 使用 `NAV_ITEMS` 而非硬编码 `menuItems`（修复 C5）

**工作量**：2 小时

### 4.3 新建 PageHeader 组件

**文件**：`frontend/src/shared/components/PageHeader.tsx`

```typescript
interface PageHeaderProps {
  title: string;          // 页面标题
  subtitle?: string;      // 灰色小字描述
  icon?: React.ReactNode; // 标题前图标
  extra?: React.ReactNode; // 右侧操作区
}
```

**用途**：每个页面顶部统一使用，替代当前散落的标题区域

**工作量**：30 分钟

### 4.4 Day 1 验收清单

- [ ] `design-tokens.ts` 定义完整（TYPOGRAPHY/SPACING/RADIUS/SHADOWS/MOTION/Z_INDEX）
- [ ] `colors.ts` 新增 GRADIENTS + SEMANTIC_COLORS
- [ ] `global.css` 新增 8+ 工具类（tech-border/scan-line/skeleton 等）
- [ ] `index.html` 引入 Orbitron 字体
- [ ] MainLayout 有 SVG Logo + Orbitron 品牌字 + 底部状态区
- [ ] MainLayout 使用 `NAV_ITEMS`（非硬编码）
- [ ] Header 有时间 + 状态 + 数据源标签
- [ ] PageHeader 组件创建并可用

---

## 五、Day 2 — 核心组件升级

### 5.1 KpiBar 升级

**文件**：`frontend/src/widgets/kpi-bar/KpiBar.tsx`
**当前问题**：4 个 Ant Design Card，每个只有图标 + 一行数字，无 sparkline、无趋势箭头、无错开入场

**升级方案**：

```
每个 KPI 卡片包含：
1. 顶部：图标 + 标签（小字灰色）
2. 中间：大数字（Orbitron 28px Black）+ 单位（12px 灰色）
3. 底部：迷你 sparkline（20px 高，ECharts mini 或 SVG）
4. 右下角：趋势箭头 + 百分比（绿色↑ / 红色↓）

视觉规格：
- 卡片用 glass-card 样式 + 顶部发光线
- hover 时 transform: translateY(-2px) + box-shadow 增强
- 数字变化时有 numberFlip 动画
- 四个卡片错开入场（fade-in-stagger）

数据扩展：
- metrics 中新增 sparklineData: number[]（最近 20 个数据点）
- metrics 中新增 trend: 'up' | 'down' | 'stable'
```

**需要**：
- `SystemMetrics` 类型扩展（新增 `sparklineData` 和 `trend` 字段）
- `dashboardMock.ts` 中生成 sparkline 数据
- 新建 `Sparkline.tsx` 迷你图表组件（用 ECharts mini 或纯 SVG）

**工作量**：2 小时

### 5.2 RiskList 升级

**文件**：`frontend/src/widgets/risk-list/RiskList.tsx`
**当前问题**：标准 Ant Design Table，视觉上和后台管理系统一样

**升级方案**：

```
改为自定义列表（不用 Table 组件）：
1. 每行：
   - 左侧：排名数字（Orbitron 大字，颜色跟随风险等级）
   - 中间：目标名称 + 详情（TTC + 场景）
   - 右侧：风险分（Orbitron 大字）+ 风险等级色块

2. 每行左侧有 3px 竖线（颜色跟随风险等级）
3. critical 行：整行有微弱红色脉冲背景
4. hover 行：背景微亮 + 左侧竖线变亮
5. 排序选择器：改为三个 Button 组（不用 Ant Select）

头部：
- "实时风险榜" 标题 + 实时数据源指示灯
- 排序按钮组（风险分 / TTC / 时间）
```

**工作量**：2 小时

### 5.3 LogStream 升级

**文件**：`frontend/src/widgets/log-stream/LogStream.tsx`
**当前问题**：功能正常但视觉平淡，日志行就是文字 + Tag，没有终端感

**升级方案**：

```
1. 容器加 tech-border（四角L型装饰）
2. 顶部标题栏加扫描线动画（scan-line class）
3. 日志行升级：
   - 时间戳用 Orbitron 字体，颜色更暗
   - 级别标签改为圆点 + 文字（不用 Ant Tag）
   - 来源用 [方括号] 包裹，青色
   - 日志内容等宽字体
   - ERROR 级别：整行左侧 2px 红色竖线 + 微弱红色背景
   - 新日志入场：从左侧滑入 + 0.3s 渐显
4. 底部状态栏：总条数 + 过滤标签 + 自动滚动开关
```

**工作量**：1.5 小时

### 5.4 ECharts 自定义主题

#### 5.4.1 新建 `src/constants/echarts-theme.ts`

```typescript
import * as echarts from 'echarts/core';

const V2X_THEME = {
  color: ['#00d4ff', '#00ff88', '#a855f7', '#f97316', '#ec4899'],
  backgroundColor: 'transparent',
  textStyle: { color: '#8892a4', fontSize: 11 },
  title: { textStyle: { color: '#e0e6f0', fontSize: 13, fontWeight: 600 } },
  legend: { textStyle: { color: '#8892a4' } },
  tooltip: {
    backgroundColor: 'rgba(10, 14, 26, 0.95)',
    borderColor: 'rgba(0, 212, 255, 0.3)',
    textStyle: { color: '#e0e6f0', fontSize: 12 },
    extraCssText: 'backdrop-filter: blur(12px); box-shadow: 0 4px 20px rgba(0,0,0,0.4);',
  },
};

echarts.registerTheme('v2x-dark', V2X_THEME);
```

#### 5.4.2 升级 `BaseChart.tsx`

**文件**：`frontend/src/entities/charts/BaseChart.tsx`
**当前**：使用 `theme={theme === 'dark' ? 'dark' : undefined}`（ECharts 内置 dark 主题）
**升级后**：使用 `theme={theme === 'dark' ? 'v2x-dark' : undefined}`

#### 5.4.3 升级 `LineChart.tsx`

**文件**：`frontend/src/entities/charts/LineChart.tsx`
**升级点**：
- 线宽 2px → 2.5px
- 面积渐变填充（从线色到透明）：`new echarts.graphic.LinearGradient(0, 0, 0, 1, [...])`
- 阈值线：虚线 + 标签
- 数据点：悬浮时显示（默认隐藏 `symbol: 'none'`）

**工作量**：1.5 小时

### 5.5 Day 2 验收清单

- [ ] KpiBar 有 sparkline + 趋势箭头 + Orbitron 字体 + 错开入场
- [ ] RiskList 是自定义列表（非 Ant Table）+ 风险竖线 + 脉冲背景
- [ ] LogStream 有终端风格 + 扫描线 + 四角装饰 + 级别色标
- [ ] ECharts 注册 `v2x-dark` 主题
- [ ] BaseChart 使用 `v2x-dark` 主题
- [ ] LineChart 有渐变面积填充

---

## 六、Day 3 — Three.js 3D 场景升级

### 6.1 安装依赖

```bash
cd frontend
npm install @react-three/postprocessing postprocessing
```

### 6.2 Road.tsx 升级

**文件**：`frontend/src/features/three-scene/Road.tsx`
**当前问题**：灰色平面 + 黄色虚线 + 人行横道，无发光材质、无交通灯、无建筑

**升级清单**：

| # | 升级项 | 当前 | 目标 |
|---|--------|------|------|
| 1 | 路面材质 | `meshStandardMaterial color="#2a2a3a"` | color: #1a1a2e + emissive: #0a0e1a + roughness: 0.8 |
| 2 | 车道线 | `meshStandardMaterial color="#ffff00" opacity=0.6` | `meshBasicMaterial` + 呼吸动画 (opacity 0.3↔0.7) |
| 3 | 停止线 | 无 | 四方向白色发光线 |
| 4 | 人行横道 | `opacity=0.8` | 加 emissive |
| 5 | 路面边线 | 无 | 道路两侧青色发光线 (#00d4ff, opacity: 0.4) |
| 6 | 地面网格 | `gridHelper args={[60, 60, '#1a1a2e', '#1a1a2e']}` | 自定义颜色 rgba(0,212,255,0.05) + 中心更亮 |

**工作量**：1.5 小时

### 6.3 Vehicle.tsx 升级

**文件**：`frontend/src/features/three-scene/Vehicle.tsx`
**当前问题**：Box 车身 + Box 座舱 + 圆柱轮子，玩具车感，无传感器、无车灯

**升级清单**：

| # | 升级项 | 当前 | 目标 |
|---|--------|------|------|
| 1 | 车身 | `boxGeometry` | `RoundedBox`（@react-three/drei）圆角 0.08 |
| 2 | 车灯 | 无 | 车头白球 (emissive 0.8) + 车尾红球 (emissive 0.6) + 自车绿球 |
| 3 | 传感器锥体 | 无 | `ConeGeometry(6, 18, 16, 1, true)` 半透明青色，仅自车 |
| 4 | 传感器环 | 无 | `RingGeometry(5.5, 6, 32)` 平贴地面，pulsing |
| 5 | 风险光环 | 无 | `RingGeometry(1.5, 1.8, 16)` 跟随 RISK_COLORS，critical 时 pulsing |
| 6 | 轮子动画 | 静态 | `useFrame` 根据速度旋转 |

**工作量**：2 小时

### 6.4 新建 3D 组件

#### 6.4.1 TrafficLight.tsx — 交通灯

**文件**：`frontend/src/features/three-scene/TrafficLight.tsx`

```
- 圆柱灯杆 (4m 高, 深灰色金属)
- 方形灯箱
- 三个球体灯 (红/黄/绿)
- 当前状态对应灯 emissiveIntensity=1.0, 其余 0.1
- 当前状态的灯加 PointLight
- Props: state ('red' | 'yellow' | 'green'), position
- 路口四角各放一个
```

**工作量**：1 小时

#### 6.4.2 BuildingWireframe.tsx — 建筑线框

**文件**：`frontend/src/features/three-scene/BuildingWireframe.tsx`

```
- BoxGeometry wireframe
- 颜色 #00d4ff, opacity: 0.15
- 实体部分极暗 (color: #0a0e1a, emissive: 0.03)
- 随机窗户灯光：小 plane 贴在建筑表面
  - 颜色 #00d4ff 或 #ffaa50
  - opacity 随机 0.2-0.6
  - 部分窗户随机闪烁
- 路口四角放 2-3 个，高度 10-28 不等
```

**工作量**：1.5 小时

#### 6.4.3 OcclusionZone.tsx — 遮挡盲区

**文件**：`frontend/src/features/three-scene/OcclusionZone.tsx`

```
- BoxGeometry 或 SphereGeometry
- color: #ff4d4f, opacity: 0.04, depthWrite: false
- 边框线框 (EdgesGeometry + LineSegments)
  - color: #ff4d4f, opacity: 0.15
- useFrame 做 pulsing (opacity 0.03↔0.07)
- 核心卖点的视觉表达："鬼探头盲区"
```

**工作量**：1 小时

#### 6.4.4 DataFlowParticles.tsx — 数据流粒子

**文件**：`frontend/src/features/three-scene/DataFlowParticles.tsx`

```
- 150 个粒子 (Points + BufferGeometry)
- 从地面 (y=0) 向天空 (y=20) 飘动
- 颜色：70% 青色 #00d4ff, 30% 绿色 #00ff88
- 大小 0.12, AdditiveBlending
- useFrame 中更新 y 坐标，超出范围重置
- 表示"路端感知数据上传云端"
```

**工作量**：1 小时

### 6.5 IntersectionScene.tsx 升级

**文件**：`frontend/src/features/three-scene/IntersectionScene.tsx`
**当前问题**：无后处理、灯光简单、无粒子、无建筑、无镜头模式

**升级清单**：

| # | 升级项 | 当前 | 目标 |
|---|--------|------|------|
| 1 | Bloom 后处理 | 无 | `<EffectComposer><Bloom luminanceThreshold={0.4} intensity={0.8} radius={0.4} /></EffectComposer>` |
| 2 | 灯光 | ambient 0.4 + directional 0.8 + point 0.6 | ambient 0.3 + directional 0.5 + point #00d4ff + point #00ff88 + point #ff4d4f |
| 3 | 雾效 | `fog args={['#0a0e1a', 40, 80]}` | `FogExp2 #050816 density 0.012` |
| 4 | 镜头模式 | 仅 OrbitControls | 新增 prop `cameraMode: 'orbit' \| 'follow' \| 'cinematic'` |
| 5 | 地面网格 | `gridHelper` | 自定义颜色 + 中心区域更亮 |
| 6 | 标题文字 | `Text` | 改用 Orbitron + 发光效果 |
| 7 | 新增组件 | 无 | TrafficLight × 4 + BuildingWireframe × 3 + OcclusionZone + DataFlowParticles |

**升级后 SceneContent 结构**：
```tsx
<>
  <ambientLight intensity={0.3} />
  <directionalLight position={[20, 30, 10]} intensity={0.5} castShadow />
  <pointLight position={[0, 12, 0]} intensity={0.8} color="#00d4ff" />
  <pointLight position={[-15, 8, -15]} intensity={0.4} color="#00ff88" />
  <pointLight position={[10, 6, -10]} intensity={0.3} color="#ff4d4f" />

  <Road />
  <DataFlowParticles />
  <OcclusionZone position={[6, 0, -5]} />

  {/* Traffic lights at 4 corners */}
  <TrafficLight position={[-8, 0, -8]} state="red" />
  <TrafficLight position={[8, 0, -8]} state="green" />
  <TrafficLight position={[-8, 0, 8]} state="green" />
  <TrafficLight position={[8, 0, 8]} state="red" />

  {/* Buildings */}
  <BuildingWireframe position={[-12, 0, -12]} height={20} />
  <BuildingWireframe position={[12, 0, -12]} height={15} />
  <BuildingWireframe position={[-12, 0, 12]} height={25} />

  {/* Vehicles, Pedestrians, Obstacles, Trajectories */}
  {sceneData.vehicles.map(...)}
  {sceneData.pedestrians.map(...)}
  {sceneData.obstacles.map(...)}

  {showLabel && <Text>...</Text>}
  <gridHelper ... />
</>

<EffectComposer>
  <Bloom luminanceThreshold={0.4} intensity={0.8} radius={0.4} />
</EffectComposer>
```

**工作量**：2 小时

### 6.6 性能控制

| 优化项 | 措施 |
|--------|------|
| Bloom 性能 | `intensity` 不超过 0.8，`radius` 不超过 0.4 |
| 粒子数 | 200 → 150（文档建议），useFrame 只更新 y 坐标 |
| 建筑 LOD | 远处建筑减少窗户数量 |
| 帧率监控 | 3D 场景加 FPS 显示（右下角） |

### 6.7 Day 3 验收清单

- [ ] `@react-three/postprocessing` 已安装
- [ ] 路面有发光车道线 + 青色边线
- [ ] 自车有传感器锥体 + 传感器环
- [ ] 车辆有车灯（前白后红，自车绿）
- [ ] 路口有 4 个交通灯
- [ ] 路口有 3 个赛博朋克建筑线框（带窗户灯光）
- [ ] 遮挡区域有红色半透明可视化 + pulsing
- [ ] 有数据流粒子（150 个，青/绿色，向上飘动）
- [ ] Bloom 后处理生效（emissive 材质有泛光）
- [ ] 3D 场景帧率 ≥30fps
- [ ] cinematic 镜头模式可用

---

## 七、Day 4 — 页面级升级 + 全屏模式

### 7.1 DashboardPage 升级

**文件**：`frontend/src/pages/dashboard/DashboardPage.tsx`
**当前布局**：KpiBar 顶部 → Row(5/13/6: RiskList | 3D | Charts) → LogStream 底部

**升级方案**：

```
1. 顶部 KpiBar：升级版 + 错开入场动画

2. 中间区域 (5/14/5 → 更突出 3D)：
   - 左 5col: RiskList（升级版自定义列表）
   - 中 14col: IntersectionScene（Bloom + 粒子 + 建筑）
     - 高度 420 → 460
     - 加 tech-border 四角装饰
     - 左上角标签 "◉ LIVE 3D TWIN"
     - 右下角 FPS 显示
   - 右 5col: 3 个图表卡片（TTC + 风险 + 制动）
     - 每个图表有标题 + 图标
     - 使用 v2x-dark ECharts 主题
     - 卡片顶部发光线

3. 底部 LogStream：升级版 + 高度 180 → 160

4. 数据来源标签：
   - live: 绿色脉冲圆点 + "LIVE"
   - mock: 黄色圆点 + "MOCK"
   - Orbitron 字体
```

**工作量**：1.5 小时

### 7.2 MonitorPage 升级

**文件**：`frontend/src/pages/monitor/MonitorPage.tsx`
**当前问题**：Demo 控制栏 + 5 个组件堆叠，信息密度高但层次不清

**升级方案**：

```
1. Demo 控制栏：
   - 改为顶部工具条样式（不是 Card）
   - 按钮自定义样式（不用 Ant Button 默认皮肤）
   - running 时绿色脉冲灯
   - FPS 显示用 Orbitron

2. 布局调整：
   - 左侧 6col: ConnectionPanel + TopicManager（堆叠）
   - 中间 12col: PerceptionCards（3 卡片横排）
   - 右侧 6col: MessagePanel

3. ConnectionPanel 升级：
   - 修复"重新连接"按钮（Day 0 已完成）
   - 连接状态用大圆点 + 文字
   - Broker 地址、Client ID 用 Orbitron mono
   - 标题改为 "WebSocket 连接面板"（修复 C7）

4. PerceptionCards 升级：
   - 3 个卡片有图标 + 标题 + 数据网格
   - 数据变化时有 numberFlip 动画

5. MessagePanel 升级：
   - 加扫描线动画
   - 消息条目加层级色标
   - 展开时 payload 用 JSON 高亮
```

**工作量**：2 小时

### 7.3 ReplayPage 升级

**文件**：`frontend/src/pages/replay/ReplayPage.tsx`
**当前状态**：回放逻辑完整，帧步进、速度控制正常

**升级方案**：

```
1. 顶部 EventTable：
   - 升级为自定义列表（同 RiskList 风格）
   - 选中行高亮 + 左侧竖线
   - 风险等级色块

2. 中间回放区域：
   - 3D 场景 + PlaybackControls
   - 回放时 3D 场景加"回放中"标签
   - 帧编号用 Orbitron

3. 底部图表：
   - 2 个 GaugeChart + 2 个 LineChart
   - 使用 v2x-dark ECharts 主题
   - GaugeChart 中心数字用 Orbitron

4. PlaybackControls 升级：
   - 进度条自定义样式
   - 播放/暂停按钮加大
   - 速度选择器用 Button Group
```

**工作量**：1.5 小时

### 7.4 EvaluationPage 升级

**文件**：`frontend/src/pages/evaluation/EvaluationPage.tsx`
**当前问题**：导出按钮 placeholder（Day 0 已修复），MetricCards 视觉平淡

**升级方案**：

```
1. 顶部报告选择器 + 导出按钮（Day 0 已修复导出）

2. MetricCards 网格升级：
   - 每个指标卡片：图标 + 名称 + 数值 + 目标值 + 达标状态
   - 达标绿色✓，未达标红色✗，未知灰色-
   - 数值用 Orbitron

3. 目标状态表：
   - 自定义列表样式（不用 Table）
   - 每行：指标名 + 当前值 + 目标 + 状态色块

4. 图表区域：
   - 基线对比：柱状图渐变色填充
   - 雷达图：填充半透明 + 边线发光
   - 消融实验：双柱状图

5. 整体：
   - 如有真实评估数据，替换 mock
   - 如无，保留 mock 但标注"演示数据"
```

**工作量**：1.5 小时

### 7.5 SettingsPage 升级

**文件**：`frontend/src/pages/settings/SettingsPage.tsx`
**当前状态**：功能完整

**升级方案**：

```
1. 分组卡片样式升级（glass-card + 顶部发光线）
2. 每个设置项：标签 + 描述 + 控件
3. 主题切换：暗色/亮色两个预览卡片 + 过渡动画
4. 保存时有 toast 提示
```

**工作量**：1 小时

### 7.6 新建 PresentationPage — 全屏演示模式

**文件**：`frontend/src/pages/presentation/PresentationPage.tsx`
**路由**：`/presentation`（独立于 MainLayout，全屏无侧边栏）

```
布局：
┌────────────────────────────────────────────────┐
│ 顶部标题栏 (72px)                                 │
│ "路云天瞳 · 数字孪生感知平台" + 实时时间            │
├──────────────────────────────┬─────────────────┤
│                              │  右侧面板 (35%)   │
│  3D 场景 (65%)                │  风险仪表盘      │
│  cinematic 镜头               │  TTC 趋势       │
│  Bloom + 粒子 + 建筑          │  风险趋势       │
│                              │  感知目标列表    │
│                              │  系统指标        │
├──────────────────────────────┴─────────────────┤
│ 底部事件时间轴 (80px)                              │
└────────────────────────────────────────────────┘

数据源：复用 dashboardStore
3D：复用 IntersectionScene，cameraMode="cinematic"
```

**路由配置**：
```typescript
// router.tsx 中新增
const PresentationPage = lazy(() => import('@/pages/presentation/PresentationPage'));

export const router = createBrowserRouter([
  {
    path: '/presentation',
    element: <PresentationPage />,  // 独立路由，不嵌套在 MainLayout 下
  },
  {
    path: '/',
    element: <MainLayout />,
    children: [ ... ],
  },
]);
```

**工作量**：3 小时

### 7.7 新建通用组件

#### 7.7.1 Toast 通知系统

**文件**：`frontend/src/shared/components/Toast/`

```
- 鬼探头事件触发时弹出全屏通知
- 红色边框 + 警告图标 + 事件描述
- 3 秒自动消失
- 位置：屏幕右上角
- 动画：从右侧滑入
```

**工作量**：1 小时

#### 7.7.2 EmptyState 组件

**文件**：`frontend/src/shared/components/EmptyState.tsx`

```
- 图标 + 标题 + 描述
- 用于无数据、无事件、无连接等场景
- 替换所有现有的"暂无数据"/"等待消息..."文字
```

**工作量**：30 分钟

#### 7.7.3 Skeleton 骨架屏组件

**文件**：`frontend/src/shared/components/Skeleton.tsx`

```
- 卡片骨架（标题 + 内容区域）
- 表格骨架（行骨架）
- 图表骨架（矩形区域）
- 加载时显示，数据到达后替换
```

**工作量**：30 分钟

### 7.8 Day 4 验收清单

- [ ] DashboardPage：3D 区域有 tech-border + LIVE 标签 + FPS 显示
- [ ] MonitorPage：Demo 控制栏工具条样式 + ConnectionPanel 标题改为 "WebSocket"
- [ ] ReplayPage：EventTable 自定义列表 + 帧编号 Orbitron
- [ ] EvaluationPage：MetricCards 有达标状态 + 图表渐变色
- [ ] SettingsPage：分组卡片 + 主题切换预览
- [ ] `/presentation` 全屏模式可用
- [ ] Toast 通知系统可用
- [ ] EmptyState 替换所有"暂无数据"文字
- [ ] Skeleton 骨架屏在加载时显示

---

## 八、Day 5 — 代码清理 + 测试 + 构建优化

### 8.1 消除 inline style

**策略**：

1. **全局工具类**（`global.css` 新增）：
   - 布局：`.flex-row`, `.flex-col`, `.flex-center`, `.gap-sm`, `.gap-md`
   - 文字：`.text-xs`, `.text-sm`, `.text-base`, `.text-lg`, `.text-bold`, `.text-accent`, `.text-secondary`
   - 间距：`.p-sm`, `.p-md`, `.p-lg`, `.m-sm`, `.m-md`
   - 圆角：`.radius-sm`, `.radius-md`

2. **CSS Modules**（每个核心组件一个 `.module.css`）：
   - `KpiBar.module.css`
   - `RiskList.module.css`
   - `LogStream.module.css`
   - `ConnectionPanel.module.css`
   - `MessagePanel.module.css`

3. **主题 CSS 变量**：
   ```css
   :root {
     --color-bg: #0a0e1a;
     --color-accent: #00d4ff;
     --color-text: #e0e6f0;
     --color-text-secondary: #8892a4;
     --spacing-sm: 8px;
     --spacing-md: 12px;
   }
   [data-theme="light"] { ... }
   ```

**工作量**：3 小时

### 8.2 清理死代码

| 文件 | 清理项 | 操作 |
|------|--------|------|
| `types/cloud.ts` | `CloudFusionResult`, `CloudAlert` | 删除 |
| `types/common.ts` | `KpiItem`, `BBox` | 删除 |
| `shared/utils/format.ts` | `formatDate`, `formatPercent`, `formatLatency`, `riskLevelToNumber`, `numberToRiskLevel` | 删除（保留 `formatTimestamp`） |
| `shared/utils/helpers.ts` | `clamp`, `lerp` | 删除 |
| `constants/config.ts` | `MQTT_DEFAULT_BROKER` | 删除（monitorStore 用 `buildWebSocketUrl()`） |
| `mock/evaluationMock.ts` | `getConfusionMatrixData` | 删除或添加 UI 展示 |

**工作量**：1 小时

### 8.3 TypeScript 严格化

**文件**：`frontend/tsconfig.json`
**修改**：
```json
{
  "compilerOptions": {
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true
  }
}
```

**注意**：修改后需要修复所有编译错误（主要是之前容忍的未使用变量）

**工作量**：1 小时

### 8.4 添加组件测试

**新建测试文件**：

| 测试文件 | 覆盖组件 | 测试内容 |
|----------|----------|----------|
| `tests/KpiBar.test.tsx` | KpiBar | 渲染 + 数字显示 + sparkline |
| `tests/RiskList.test.tsx` | RiskList | 排序 + 行点击 |
| `tests/LogStream.test.tsx` | LogStream | 过滤 + 自动滚动 |
| `tests/GaugeChart.test.tsx` | GaugeChart | 数值显示 + 阈值颜色 |

**依赖安装**：
```bash
npm install -D @testing-library/react @testing-library/jest-dom
```

**工作量**：4 小时（可压缩）

### 8.5 构建优化

#### 8.5.1 Gzip 压缩

**文件**：`frontend/vite.config.ts`
**新增**：
```typescript
import viteCompression from 'vite-plugin-compression';

export default defineConfig({
  plugins: [
    react(),
    viteCompression({
      verbose: true,
      disable: false,
      threshold: 10240,
      algorithm: 'gzip',
      ext: '.gz',
    }),
  ],
  // ... 现有配置
});
```

**依赖**：
```bash
npm install -D vite-plugin-compression
```

#### 8.5.2 包分析

**依赖**：
```bash
npm install -D rollup-plugin-visualizer
```

**配置**：
```typescript
import { visualizer } from 'rollup-plugin-visualizer';

export default defineConfig({
  plugins: [
    react(),
    visualizer({ open: true, filename: 'dist/stats.html' }),
  ],
});
```

#### 8.5.3 Three.js Tree-shaking

确保只导入用到的 Three.js 类：
```typescript
// ✅ 正确：按需导入
import * as THREE from 'three';

// ❌ 避免：全量导入
// import THREE from 'three';
```

**工作量**：1 小时

### 8.6 性能优化

#### 8.6.1 WebSocket 消息节流

**文件**：`frontend/src/store/dashboardStore.ts`
**优化**：
- 感知消息 10Hz → 前端限 5Hz 更新（每 200ms 合并一次）
- 日志消息：批量添加（每 500ms flush）

#### 8.6.2 Store 选择器优化

**当前问题**：`const { metrics, riskItems, ... } = useDashboardStore()` 解构多个字段，任意字段更新都触发重渲染

**优化方案**：
```typescript
// ✅ 细粒度选择器
const metrics = useDashboardStore(s => s.metrics);
const riskItems = useDashboardStore(s => s.riskItems);

// ❌ 当前：全量解构
// const { metrics, riskItems, ... } = useDashboardStore();
```

#### 8.6.3 虚拟列表

**依赖**：
```bash
npm install react-window @types/react-window
```

**应用**：RiskList 和 LogStream 在数据量大时使用虚拟滚动

**工作量**：2 小时

### 8.7 Day 5 验收清单

- [ ] 无 inline style（或极少，仅动态值）
- [ ] 死代码全部清理（6 个类型/函数/常量删除）
- [ ] `tsconfig.json` 严格模式开启，`npm run build` 无错误
- [ ] 4 个组件测试文件创建且通过
- [ ] 构建产物有 `.gz` 压缩文件
- [ ] 包分析报告生成
- [ ] Store 使用细粒度选择器
- [ ] `npm run build` 无错误 + 无 warning
- [ ] 逐页检查 5 个页面 + `/presentation` 全部正常

---

## 九、文档修复计划

### 9.1 修复清单

| # | 文件 | 问题 | 修复方案 | 工作量 | 优先级 |
|---|------|------|----------|--------|--------|
| D1 | `docs/ARCHITECTURE.md` | 引用 InfluxDB，实际用 SQLite | 全文搜索替换 InfluxDB → SQLite WAL | 30 分钟 | P2 |
| D2 | `frontend/README.md` | 描述"纯前端 UI demo + mock 数据" | 更新为"已接真实后端，支持 live/mock 双模式" | 20 分钟 | P2 |
| D3 | `docs/MVP_REQUIREMENTS.md` | DoD 复选框未更新 | 根据 GOAL.md 更新勾选状态 | 20 分钟 | P3 |
| D4 | 根目录 | 无 README.md | 新建项目级 README（项目介绍+快速启动+架构图+技术栈） | 1 小时 | P2 |
| D5 | `docs/WEEKLY_PLAN.md` | 7 天计划，实际远超 | 顶部标注 "⚠️ Historical document" | 5 分钟 | P3 |

### 9.2 根目录 README.md 结构

```markdown
# 路云天瞳 — 分布式多智能体车路协同遮挡感知平台

## 简介
（1 段话描述核心场景和技术架构）

## 快速启动
### 前置条件
### 一键启动（Windows）
### 一键启动（Linux/Mac）
### 手动启动

## 系统架构
（三智能体架构图）

## 技术栈
（表格）

## 项目结构
（目录树）

## 开发指南
### 后端开发
### 前端开发
### 测试

## 部署
### Docker Compose
### CI/CD

## 文档索引
```

**工作量**：1 小时

---

## 十、M1/M2 推进计划（答辩加分项）

### 10.1 M1 剩余项

| # | 任务 | 工作量 | 说明 |
|---|------|--------|------|
| M1-1 | Windows 本地 Mosquitto 安装 + 外部 broker 验证 | 2 小时 | M1 最后一个未勾选项 |
| M1-2 | Docker 容器实际构建 + 启动验证 | 3 小时 | GOAL.md 明确标注未完成 |
| M1-3 | 三智能体本地端到端实时演示 | 2 小时 | 非 CI 环境的完整演示 |

### 10.2 M2 剩余项

| # | 任务 | 工作量 | 说明 |
|---|------|--------|------|
| M2-1 | 下载 DAIR-V2X 数据集 | 依赖网络 | `real_candidate_count=0` → 有真实数据 |
| M2-2 | ST-GNN 训练（8 epochs smoke） | 2-4 小时 | algorithm.yml CI 已验证可行 |
| M2-3 | 真实评估结果生成 | 2 小时 | 替换 demo 样本评估结果 |
| M2-4 | YOLOv8 真实场景推理测试 | 2 小时 | 验证检测精度和速度 |

### 10.3 达标验证目标

| 指标 | 目标 | 当前状态 |
|------|------|----------|
| ADE | < 1.0m | ❌ 未验证 |
| FDE | < 2.0m | ❌ 未验证 |
| Occ-ADE | < 1.5m | ❌ 未验证 |
| Occ-Acc | ≥ 70% | ❌ 未验证 |
| FPS | ≥ 10 | ❌ 未验证 |
| E2E-Lat | < 100ms | ❌ 未验证 |
| Lead-Time | ≥ 1.5s | ❌ 未验证 |

---

## 十一、答辩准备清单

### 11.1 演示流程（3 分钟）

| 步骤 | 时间 | 内容 |
|------|------|------|
| 1. 开场 | 30s | `/presentation` 全屏模式投影 → 介绍系统整体架构 |
| 2. 3D 孪生 | 60s | Three.js 3D 场景 → 鬼探头场景 → 遮挡区域红色高亮 → 自车感知锥体 → 数据流粒子 |
| 3. 实时数据 | 45s | `/monitor` → 启动 Demo → WebSocket live 数据流 → 感知目标列表 + 消息面板 |
| 4. 事件回放 | 30s | `/replay` → 选中鬼探头事件 → 帧步进回放 → TTC 仪表盘 + 风险趋势 |
| 5. 模型评估 | 30s | `/evaluation` → 展示指标对比 + 消融实验 |
| 6. 收尾 | 15s | 回到大屏 → 总结 |

### 11.2 答辩前自检

- [ ] `python -m pytest tests/` 全部通过
- [ ] `cd frontend && npm run build` 无错误
- [ ] `scripts/start_demo.ps1` 一键启动成功
- [ ] 浏览器访问 `/` 数据正常更新（live 指示灯）
- [ ] 浏览器访问 `/monitor` Demo 控制可用
- [ ] 浏览器访问 `/replay` 事件回放正常
- [ ] 浏览器访问 `/evaluation` 评估数据展示
- [ ] 浏览器访问 `/presentation` 全屏模式正常
- [ ] 3D 场景帧率 ≥30fps
- [ ] WebSocket 断开后自动重连
- [ ] "重新连接"按钮功能正常
- [ ] CORS 无跨域错误
- [ ] 导出功能正常下载 JSON
- [ ] 演示分辨率与投影仪匹配（1920×1080）

---

## 十二、依赖安装总览

### 12.1 新增生产依赖

```bash
cd frontend
npm install @react-three/postprocessing postprocessing
npm install react-window
```

### 12.2 新增开发依赖

```bash
npm install -D @types/react-window
npm install -D rollup-plugin-visualizer
npm install -D vite-plugin-compression
npm install -D @testing-library/react @testing-library/jest-dom
```

### 12.3 完整依赖清单

| 依赖 | 类型 | 用途 | 版本 |
|------|------|------|------|
| `@react-three/postprocessing` | prod | 3D Bloom 后处理 | latest |
| `postprocessing` | prod | postprocessing 底层 | latest |
| `react-window` | prod | 虚拟列表 | latest |
| `@types/react-window` | dev | react-window 类型 | latest |
| `rollup-plugin-visualizer` | dev | 包大小分析 | latest |
| `vite-plugin-compression` | dev | gzip 压缩 | latest |
| `@testing-library/react` | dev | 组件测试 | latest |
| `@testing-library/jest-dom` | dev | DOM 断言 | latest |

---

## 十三、新建文件清单

### 13.1 设计系统

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `frontend/src/constants/design-tokens.ts` | 新建 | 排版/间距/圆角/阴影/动效/Z-index 常量 |
| `frontend/src/constants/echarts-theme.ts` | 新建 | ECharts v2x-dark 自定义主题 |

### 13.2 3D 组件

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `frontend/src/features/three-scene/TrafficLight.tsx` | 新建 | 交通灯组件 |
| `frontend/src/features/three-scene/BuildingWireframe.tsx` | 新建 | 赛博朋克建筑线框 |
| `frontend/src/features/three-scene/OcclusionZone.tsx` | 新建 | 遮挡盲区可视化 |
| `frontend/src/features/three-scene/DataFlowParticles.tsx` | 新建 | 数据流粒子 |

### 13.3 通用组件

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `frontend/src/shared/components/PageHeader.tsx` | 新建 | 统一页面头部 |
| `frontend/src/shared/components/EmptyState.tsx` | 新建 | 空状态组件 |
| `frontend/src/shared/components/Skeleton.tsx` | 新建 | 骨架屏组件 |
| `frontend/src/shared/components/Sparkline.tsx` | 新建 | 迷你折线图 |
| `frontend/src/shared/components/Toast/` | 新建目录 | Toast 通知系统 |

### 13.4 页面

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `frontend/src/pages/presentation/PresentationPage.tsx` | 新建 | 全屏演示模式 |

### 13.5 测试

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `frontend/tests/KpiBar.test.tsx` | 新建 | KpiBar 组件测试 |
| `frontend/tests/RiskList.test.tsx` | 新建 | RiskList 组件测试 |
| `frontend/tests/LogStream.test.tsx` | 新建 | LogStream 组件测试 |
| `frontend/tests/GaugeChart.test.tsx` | 新建 | GaugeChart 组件测试 |

### 13.6 文档

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `README.md`（根目录） | 新建 | 项目级 README |
| `frontend/src/shared/components/Toast/Toast.tsx` | 新建 | Toast 组件实现 |
| `frontend/src/shared/components/Toast/Toast.module.css` | 新建 | Toast 样式 |

### 13.7 CSS Modules

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `frontend/src/widgets/kpi-bar/KpiBar.module.css` | 新建 | KpiBar 样式 |
| `frontend/src/widgets/risk-list/RiskList.module.css` | 新建 | RiskList 样式 |
| `frontend/src/widgets/log-stream/LogStream.module.css` | 新建 | LogStream 样式 |
| `frontend/src/widgets/connection-panel/ConnectionPanel.module.css` | 新建 | ConnectionPanel 样式 |
| `frontend/src/widgets/message-panel/MessagePanel.module.css` | 新建 | MessagePanel 样式 |

---

## 十四、修改文件清单

### 14.1 后端修改

| 文件路径 | 修改内容 |
|----------|----------|
| `src/cloud_twin/api.py` | 修复 CORS（移除 `"*"`）+ 修复 404 响应格式（2 处改用 `raise HTTPException`） |
| `src/cloud_twin/data_store.py` | 修复 SQL 注入（COUNT 查询改用参数化 `?` 占位符） |

### 14.2 前端修改

| 文件路径 | 修改内容 |
|----------|----------|
| `frontend/src/widgets/connection-panel/ConnectionPanel.tsx` | 修复重连按钮 + 标题改为 "WebSocket" + 样式升级 |
| `frontend/src/store/monitorStore.ts` | 修复 toggleConnection + 移除 MQTT_DEFAULT_BROKER 硬编码 |
| `frontend/src/pages/evaluation/EvaluationPage.tsx` | 修复导出按钮 + MetricCards 升级 |
| `frontend/src/constants/colors.ts` | 新增 GRADIENTS + SEMANTIC_COLORS |
| `frontend/src/app/styles/global.css` | 新增 8+ 工具类 + 升级 body 背景 |
| `frontend/src/app/layout/MainLayout.tsx` | Logo + 菜单 + Header + 底部状态区升级 |
| `frontend/src/entities/charts/BaseChart.tsx` | 使用 v2x-dark 主题 |
| `frontend/src/entities/charts/LineChart.tsx` | 渐变面积 + 发光效果 |
| `frontend/src/widgets/kpi-bar/KpiBar.tsx` | Sparkline + 趋势箭头 + Orbitron |
| `frontend/src/widgets/risk-list/RiskList.tsx` | 改为自定义列表 |
| `frontend/src/widgets/log-stream/LogStream.tsx` | 终端风格 + 扫描线 |
| `frontend/src/widgets/metric-cards/MetricCards.tsx` | 达标状态 + Orbitron |
| `frontend/src/widgets/event-table/EventTable.tsx` | 自定义列表风格 |
| `frontend/src/widgets/playback-controls/PlaybackControls.tsx` | 自定义进度条 |
| `frontend/src/widgets/perception-cards/PerceptionCards.tsx` | numberFlip 动画 |
| `frontend/src/widgets/message-panel/MessagePanel.tsx` | 扫描线 + JSON 高亮 |
| `frontend/src/features/three-scene/IntersectionScene.tsx` | Bloom + 灯光 + 镜头模式 + 新组件 |
| `frontend/src/features/three-scene/Road.tsx` | 发光材质 + 边线 + 停止线 |
| `frontend/src/features/three-scene/Vehicle.tsx` | RoundedBox + 车灯 + 传感器锥体 |
| `frontend/src/features/three-scene/Pedestrian.tsx` | 视觉微调 |
| `frontend/src/features/three-scene/Obstacle.tsx` | 遮挡盲区指示升级 |
| `frontend/src/features/three-scene/TrajectoryLine.tsx` | 发光效果 |
| `frontend/src/pages/dashboard/DashboardPage.tsx` | 布局调整 + tech-border |
| `frontend/src/pages/monitor/MonitorPage.tsx` | 工具条样式 |
| `frontend/src/pages/replay/ReplayPage.tsx` | 自定义列表 + 标签 |
| `frontend/src/pages/settings/SettingsPage.tsx` | 分组卡片 + 主题预览 |
| `frontend/src/app/router.tsx` | 新增 /presentation 路由 |
| `frontend/src/store/dashboardStore.ts` | sparklineData + 细粒度选择器 |
| `frontend/src/types/metrics.ts` | SystemMetrics 扩展 sparklineData/trend |
| `frontend/src/types/cloud.ts` | 删除 CloudFusionResult/CloudAlert |
| `frontend/src/types/common.ts` | 删除 KpiItem/BBox |
| `frontend/src/shared/utils/format.ts` | 删除 5 个未使用函数 |
| `frontend/src/shared/utils/helpers.ts` | 删除 clamp/lerp |
| `frontend/src/constants/config.ts` | 删除 MQTT_DEFAULT_BROKER |
| `frontend/src/mock/dashboardMock.ts` | 新增 sparkline 数据生成 |
| `frontend/tsconfig.json` | noUnusedLocals/noUnusedParameters → true |
| `frontend/vite.config.ts` | gzip 压缩 + 包分析 |
| `frontend/index.html` | 引入 Orbitron 字体 |
| `frontend/package.json` | 新增依赖 |

### 14.3 文档修改

| 文件路径 | 修改内容 |
|----------|----------|
| `docs/ARCHITECTURE.md` | InfluxDB → SQLite |
| `frontend/README.md` | 更新描述 |
| `docs/MVP_REQUIREMENTS.md` | 更新 DoD 复选框 |
| `docs/WEEKLY_PLAN.md` | 标记为 historical |
| `README.md`（根目录新建） | 项目级 README |

---

## 十五、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 3D 场景帧率不达标 | 中 | 高 | Bloom intensity 控制 + 粒子数 150 + 建筑 LOD |
| Orbitron 字体加载慢 | 低 | 中 | `font-display: swap` + preconnect |
| TypeScript 严格化导致大量编译错误 | 中 | 中 | 分批修复，先修复当前文件再开启 |
| 组件测试环境搭建复杂 | 中 | 低 | 优先保证 `npm run build` 通过，测试可降级 |
| Docker 容器构建失败 | 中 | 中 | 优先保证本地 `start_demo.ps1` 可用 |
| DAIR-V2X 数据集下载失败 | 高 | 低 | 保留 mock 数据，标注"演示数据" |

---

## 十六、验收标准总表

### 16.1 设计系统

- [ ] `design-tokens.ts` 定义完整（TYPOGRAPHY/SPACING/RADIUS/SHADOWS/MOTION/Z_INDEX）
- [ ] 所有组件引用 design tokens，无硬编码数值
- [ ] `global.css` 有 10+ 工具类
- [ ] Orbitron 字体用于数字和标题

### 16.2 布局

- [ ] 侧边栏有 Logo 图标 + 品牌文字 + 底部状态区
- [ ] Header 有时间 + 状态 + 数据源标签
- [ ] 每页有统一 PageHeader
- [ ] 页面切换有动画

### 16.3 组件

- [ ] KpiBar 有 sparkline + 趋势箭头
- [ ] RiskList 是自定义列表（非 Ant Table）
- [ ] LogStream 有终端风格 + 扫描线
- [ ] ECharts 使用 v2x-dark 自定义主题
- [ ] 图表有渐变面积 + 发光

### 16.4 3D 场景

- [ ] 路面有发光车道线
- [ ] 自车有传感器锥体
- [ ] 路口有交通灯和建筑线框
- [ ] 遮挡区域有红色半透明可视化
- [ ] 有数据流粒子
- [ ] Bloom 泛光效果
- [ ] cinematic 镜头模式
- [ ] 帧率 ≥30fps

### 16.5 页面

- [ ] 5 个页面全部升级
- [ ] 全屏演示模式 `/presentation` 可用
- [ ] 重连按钮功能正常
- [ ] 导出功能实现
- [ ] 空状态和骨架屏

### 16.6 性能

- [ ] 3D 场景 ≥30fps
- [ ] 列表用虚拟滚动
- [ ] Store 选择器细粒度
- [ ] 构建产物有 gzip

### 16.7 代码质量

- [ ] 无 inline style（或极少）
- [ ] 无死代码
- [ ] 无 any 类型
- [ ] 组件测试 4 个
- [ ] `tsconfig.json` 严格模式

### 16.8 Bug 修复

- [ ] SQL 注入已修复
- [ ] CORS 配置已修复
- [ ] 404 响应格式已修复
- [ ] 重连按钮已修复
- [ ] toggleConnection 已修复
- [ ] 导出按钮已修复

### 16.9 文档

- [ ] 根目录 README.md 已创建
- [ ] ARCHITECTURE.md 已更新（InfluxDB → SQLite）
- [ ] frontend/README.md 已更新
- [ ] MVP_REQUIREMENTS.md DoD 已更新

---

## 附录 A：关键文件索引

```
项目根: e:\路云天瞳\luyuntiantong\

后端源码:
├── src/cloud_twin/              # 云端孪生（7 文件）
│   ├── api.py                   # FastAPI 路由 ← 修复 CORS + 404
│   ├── data_store.py            # SQLite ← 修复 SQL 注入
│   ├── demo_engine.py           # 仿真引擎
│   ├── cloud_agent.py           # MQTT 订阅者
│   ├── in_memory_demo.py        # 无 MQTT 演示
│   ├── mqtt_broker_demo.py      # 外部 broker 演示
│   └── runtime_config.py        # 运行时配置
├── src/roadside_perception/     # 路端感知（7 文件）
├── src/vehicle_decision/        # 车端决策（4 文件）
├── src/communication/           # 通信层（4 文件）
├── src/dataset/                 # 数据集评估（5 文件）
├── src/utils/                   # 工具层（3 文件）
├── src/evaluation_targets.py    # M2 目标规格
└── src/evaluation_lead_time.py  # Lead-time 计算

前端源码:
├── frontend/src/
│   ├── app/                     # 入口 + 路由 + 布局 + 样式
│   │   ├── router.tsx           # ← 新增 /presentation 路由
│   │   ├── layout/MainLayout.tsx # ← 升级 Logo + Header
│   │   └── styles/global.css    # ← 升级工具类
│   ├── pages/                   # 5 页面 + 404
│   │   ├── dashboard/           # ← 升级布局
│   │   ├── monitor/             # ← 修复 Bug + 升级
│   │   ├── replay/              # ← 升级列表
│   │   ├── evaluation/          # ← 修复导出 + 升级
│   │   ├── settings/            # ← 升级样式
│   │   ├── not-found/
│   │   └── presentation/        # ← 新建全屏模式
│   ├── widgets/                 # 10 组件
│   │   ├── kpi-bar/             # ← Sparkline 升级
│   │   ├── risk-list/           # ← 自定义列表
│   │   ├── log-stream/          # ← 终端风格
│   │   ├── connection-panel/    # ← 修复重连按钮
│   │   ├── metric-cards/        # ← 达标状态
│   │   ├── event-table/         # ← 自定义列表
│   │   ├── playback-controls/   # ← 自定义进度条
│   │   ├── perception-cards/    # ← numberFlip
│   │   ├── message-panel/       # ← 扫描线
│   │   └── topic-manager/
│   ├── entities/charts/         # 4 图表
│   │   ├── BaseChart.tsx        # ← v2x-dark 主题
│   │   ├── LineChart.tsx        # ← 渐变面积
│   │   ├── BarChart.tsx
│   │   └── GaugeChart.tsx
│   ├── features/three-scene/    # 3D 组件
│   │   ├── IntersectionScene.tsx # ← Bloom + 粒子 + 建筑
│   │   ├── Road.tsx             # ← 发光材质
│   │   ├── Vehicle.tsx          # ← 传感器锥体 + 车灯
│   │   ├── Pedestrian.tsx
│   │   ├── Obstacle.tsx
│   │   ├── TrajectoryLine.tsx
│   │   ├── TrafficLight.tsx     # ← 新建
│   │   ├── BuildingWireframe.tsx # ← 新建
│   │   ├── OcclusionZone.tsx    # ← 新建
│   │   └── DataFlowParticles.tsx # ← 新建
│   ├── store/                   # 6 Zustand store
│   │   ├── dashboardStore.ts    # ← sparkline + 选择器
│   │   ├── monitorStore.ts      # ← 修复 toggleConnection
│   │   ├── replayStore.ts
│   │   ├── evaluationStore.ts
│   │   ├── settingsStore.ts
│   │   └── dashboardDataSource.ts
│   ├── services/                # 6 API/WS 服务
│   ├── types/                   # 7 类型文件 ← 清理死代码
│   ├── mock/                    # 5 mock 文件
│   ├── shared/                  # 组件 + hooks + utils
│   │   └── components/          # ← 新建 PageHeader/Toast/EmptyState/Skeleton/Sparkline
│   └── constants/               # 颜色 + 配置
│       ├── colors.ts            # ← 新增 GRADIENTS + SEMANTIC_COLORS
│       ├── config.ts            # ← 清理未使用常量
│       ├── design-tokens.ts     # ← 新建
│       └── echarts-theme.ts     # ← 新建

配置:
├── configs/                     # 4 个 YAML
├── deployment/                  # Docker + Nginx + Mosquitto
├── docker-compose.yml           # 6 服务
├── requirements.txt             # 完整依赖
├── requirements-ci.txt          # 轻量 CI 依赖
└── environment-algorithm.yml    # Conda 算法环境

脚本:
├── scripts/start_demo.ps1       # 一键 Demo
├── scripts/start_mqtt_demo.ps1  # 一键 MQTT 三智能体
├── scripts/verify_all.ps1       # 全量验证
└── ... (共 22 个脚本)

文档:
├── GOAL.md                      # 项目目标
├── 启动.md                       # 启动指南
├── 项目介绍.md                   # 项目介绍
├── README.md                    # ← 新建（根目录）
└── docs/                        # 技术文档
    ├── ARCHITECTURE.md          # ← 修复 InfluxDB → SQLite
    ├── API_SPEC.md
    ├── MVP_REQUIREMENTS.md      # ← 更新 DoD
    └── ...
```

---

## 附录 B：每日时间分配建议

### Day 0（紧急修复）— 总计 2 小时

| 时段 | 任务 | 时长 |
|------|------|------|
| 上午 | 后端 3 Bug 修复（SQL注入 + CORS + 404） | 1.25 小时 |
| 上午 | 前端 3 Bug 修复（重连 + toggle + 导出） | 0.75 小时 |
| 下午 | 验证 + 回归测试 | 0.5 小时 |

### Day 1（设计系统 + 布局）— 总计 4.5 小时

| 时段 | 任务 | 时长 |
|------|------|------|
| 上午 | design-tokens.ts + colors.ts 升级 | 0.75 小时 |
| 上午 | global.css 升级 + Orbitron 字体 | 1.25 小时 |
| 下午 | MainLayout 升级 | 2 小时 |
| 下午 | PageHeader 组件 | 0.5 小时 |

### Day 2（组件升级）— 总计 7 小时

| 时段 | 任务 | 时长 |
|------|------|------|
| 上午 | KpiBar 升级（含 Sparkline） | 2 小时 |
| 上午 | RiskList 升级 | 2 小时 |
| 下午 | LogStream 升级 | 1.5 小时 |
| 下午 | ECharts 主题 + 图表升级 | 1.5 小时 |

### Day 3（3D 场景）— 总计 8 小时

| 时段 | 任务 | 时长 |
|------|------|------|
| 上午 | 安装依赖 + Road.tsx 升级 | 2 小时 |
| 上午 | Vehicle.tsx 升级 | 2 小时 |
| 下午 | TrafficLight + BuildingWireframe + OcclusionZone + DataFlowParticles | 3 小时 |
| 下午 | IntersectionScene 升级 + 性能调试 | 1.5 小时 |

### Day 4（页面 + 全屏）— 总计 10.5 小时

| 时段 | 任务 | 时长 |
|------|------|------|
| 上午 | DashboardPage + MonitorPage 升级 | 3.5 小时 |
| 上午 | ReplayPage + EvaluationPage + SettingsPage 升级 | 3 小时 |
| 下午 | PresentationPage 全屏模式 | 3 小时 |
| 下午 | Toast + EmptyState + Skeleton 通用组件 | 1.5 小时 |

### Day 5（清理 + 测试 + 优化）— 总计 11 小时

| 时段 | 任务 | 时长 |
|------|------|------|
| 上午 | 消除 inline style + CSS Modules | 3 小时 |
| 上午 | 清理死代码 + TypeScript 严格化 | 1.5 小时 |
| 下午 | 组件测试（4 个） | 3.5 小时 |
| 下午 | 构建优化（gzip + 包分析） + 性能优化 | 2 小时 |
| 下午 | 全量验证 + 逐页检查 | 1.5 小时 |

### 总计：约 43 小时（5 个工作日 + 1 个半天紧急修复）

---

*本文档基于 2026-07-08 代码审查结果生成，所有问题项均已在实际代码中确认。后续开发以本文档为准。*
