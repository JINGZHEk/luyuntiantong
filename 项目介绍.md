# V2X 分布式多智能体车路协同遮挡感知平台 — 项目介绍

> 项目名称：分布式多智能体车路协同遮挡感知平台（V2X Ghost-Probe Platform）
> 版本：v0.1
> 最后更新：2026-07-01

---

## 一、项目概述

### 1.1 一句话介绍

本项目是一个面向自动驾驶"鬼探头"长尾危险工况的 **车路云协同主动安全防御系统**。路侧感知端提前发现被遮挡的行人，通过 V2X 通信传递给车端，车端在单车感知来得及之前完成风险预警与分级制动，整个过程由云端孪生实时可视化并持久化。

### 1.2 项目定位

| 维度       | 说明                                                      |
|------------|-----------------------------------------------------------|
| 领域       | 自动驾驶安全 · 车路协同（V2X）· 数字孪生                  |
| 目标场景   | 城市交叉路口"鬼探头"——行人从路边停放车辆/遮挡物后突然走入车道 |
| 技术路线   | 仿真优先，数据驱动，边-车-云分布式多智能体架构              |
| 开发阶段   | MVP 原型（M0 阶段：单帧检测 → MQTT → 前端渲染闭环）        |

---

## 二、背景与问题

### 2.1 为什么是"鬼探头"

"鬼探头"是行人从路边遮挡物后突然进入机动车道，驾驶员在发现时已来不及做出有效反应。这是城市道路最高频、致死率极高的长尾危险工况之一。

单车感知系统的根本局限：
- **视距受遮挡**：摄像头/激光雷达的物理视场被路边障碍物切断
- **识别滞后**：行人进入开放视野后才被检测，决策窗口严重不足
- **无提前预警**：单车无法在行人出现前预判其运动趋势

### 2.2 车路协同的价值

- **路侧相机位置更高、视角更广**，可从侧向提前捕获即将出现的行人
- **边缘节点实时推理**（YOLOv8 + 轨迹预测），低延迟输出感知结果
- **V2X 低时延通信**（MQTT，≤100ms 链路），把路侧感知前传至车端
- **车端融合决策**：路侧感知 + 自车状态 → 分级制动决策
- **云端孪生**：3D 实时可视化 + 数据持久化 + 历史回放 + 事件溯源

---

## 三、系统架构

### 3.1 整体拓扑

```
┌─────────────────────────────────────────────────────────────────────┐
│                         云端孪生平台 (Cloud)                         │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────────────────┐ │
│  │  FastAPI    │  │  SQLite      │  │  React + Three.js 前端      │ │
│  │  后端服务    │  │  数据存储     │  │  (Vite + WebSocket + ECharts)│ │
│  │  Port 8000  │  │  data/*.db   │  │  Port 3000/5173            │ │
│  └──────┬──────┘  └──────┬───────┘  └────────────────┬───────────┘ │
│         └────────────────┼───────────────────────────┘             │
└──────────────────────────┼─────────────────────────────────────────┘
                           │ MQTT (port 1883) + WebSocket
                   ┌───────┴───────┐
                   │  MQTT Broker   │   Mosquitto
                   └───┬───────────┘
                       │
      ┌────────────────┼─────────────────┐
      │                │                 │
┌─────▼───────┐  ┌─────▼──────┐  ┌──────▼─────────────┐
│ 路侧感知智能体│  │ 车端决策   │  │  回放引擎            │
│ (Roadside   │  │ 智能体      │  │ (Replay Engine)     │
│  Agent)     │  │(Vehicle    │  │                    │
│            │  │  Agent)    │  │ 驱动路侧逐帧回放      │
│ ┌──────────┐│  │            │  │ 向 MQTT 推送感知数据 │
│ │YOLOv8    ││  │ ┌──────────┐│  └────────────────────┘
│ │检测器     ││  │ │TTC风险   ││
│ └─────┬────┘│  │ │评估器     ││
│ ┌─────▼────┐│  │ └─────┬────┘│
│ │遮挡估计器 ││  │ ┌─────▼────┐│
│ └─────┬────┘│  │ │分级制动   ││
│ ┌─────▼────┐│  │ │控制器     ││
│ │轨迹预测器 ││  │ └─────┬────┘│
│ └─────┬────┘│  │ ┌─────▼────┐│
│ ┌─────▼────┐│  │ │退化管理器 ││
│ │MQTT发布端 ││  │ │(Fallback)││
│ └──────────┘│  │ └──────────┘│
│            │  │             │
│ [GPU推理设备]│  │ [车端ECU]   │
└────────────┘  └─────────────┘
```

### 3.2 核心模块职责

| 模块                    | 路径                        | 职责                              |
|-------------------------|-----------------------------|-----------------------------------|
| 路侧感知智能体            | `src/roadside_perception/`  | 视频帧检测 → 遮挡估计 → 轨迹预测 → MQTT 发布 |
| 回放引擎                  | `src/roadside_perception/replay_engine.py` | 以配置 FPS 驱动路侧逐帧回放，自动生成仿真场景 |
| 车端决策智能体            | `src/vehicle_decision/`     | 接收路侧感知 → TTC 风险评估 → 分级制动 → 状态发布 |
| 退化管理器                | `src/vehicle_decision/fallback_manager.py` | 通信中断时进入降级模式，恢复后渐进融合 |
| 云端孪生                  | `src/cloud_twin/`           | 订阅全部 MQTT 消息 → SQLite 持久化 → FastAPI 服务 → WebSocket 推送 |
| 通信层                    | `src/communication/`        | MQTT 客户端封装 + 消息协议 Dataclass |
| 前端                      | `frontend/`                 | React + Three.js 3D 场景 + ECharts 监控面板 + AntD 控制台 |

---

## 四、各模块详解

### 4.1 路侧感知智能体（Roadside Perception Agent）

**入口**：`src/roadside_perception/roadside_agent.py`

#### 数据流

```
视频帧 / 标注帧 (10 FPS)
      │
      ▼
  Detector.detect() / detect_from_annotations()
      │   返回：[{bbox, class, confidence, track_id}, ...]
      ▼
  OcclusionEstimator.estimate(detection)
      │   返回：occlusion_level ∈ {0=none, 1=light, 2=moderate, 3=heavy}
      ▼
  TrajectoryPredictor.update(track_id, world_pos)
  TrajectoryPredictor.predict(track_id, occlusion_level)
      │   返回：[[x1,y1], [x2,y2], ..., [xT,yT]]   T=30步（3秒@10Hz）
      ▼
  PerceptionMessage
      │
      ▼
  MQTT 发布 → v2x/{scene_id}/roadside/{node_id}/perception
  MQTT 发布 → v2x/{scene_id}/roadside/{node_id}/heartbeat  (每5秒)
```

#### 关键类

| 类               | 文件                     | 说明                                       |
|------------------|--------------------------|--------------------------------------------|
| `Detector`       | `detector.py`            | 封装 YOLOv8，支持 simulate 模式（标注帧恢复），track_id 关联 |
| `OcclusionEstimator` | `occlusion_estimator.py` | 根据检测框与历史轨迹估算遮挡等级（0~3）   |
| `TrajectoryPredictor` | `trajectory_predictor.py` | 维护每个 track 历史位置，滑动窗口 + 指数平滑预测未来轨迹 |
| `RoadsideAgent`  | `roadside_agent.py`      | 串联以上模块，按 FPS 驱动，每帧处理后 MQTT 发布 |
| `ReplayEngine`   | `replay_engine.py`       | 逐帧驱动路侧 agent 回放，支持目录/JSON/合成三种数据源 |

#### 仿真回放（ReplayEngine）

- 以 **10 FPS** 驱动路侧 agent 逐帧处理数据
- 支持 **目录格式**（每帧一个 JSON）和 **JSON 列表格式**
- 无真实数据时自动生成 **"鬼探头"合成场景**：
  - 停靠车辆挡住行人 → 行人第 20 帧出现 → 第 40 帧进入车道 → 第 50 帧完全暴露

---

### 4.2 车端决策智能体（Vehicle Decision Agent）

**入口**：`src/vehicle_decision/vehicle_agent.py`

#### 决策数据流

```
MQTT 订阅 v2x/{scene_id}/roadside/+/perception
      │
      ▼
  构建 predicted_trajs: {track_id: [[x,y], ...]}
      │
      ▼
  RiskAssessor.assess(ego_position, ego_velocity, objects, predicted_trajs)
      │   返回 RiskResult: {level, ttc, collision_prob, target_track_id}
      │
      │   TTC = distance / relative_speed
      │        （仅当 lateral_distance < 3.5m 才计入）
      │   collision_prob = sigmoid(-(TTC - 2.0) / 0.8)
      │   Risk Level: SAFE / WARNING / DANGER / EMERGENCY
      ▼
  BrakeController.compute(risk_level, speed)
      │   SAFE → 0.0 m/s²
      │   WARNING → 2.0 m/s²
      │   DANGER → 5.0 m/s²
      │   EMERGENCY → 8.0 m/s²
      ▼
  更新自车状态（运动学积分）
      │
      ▼
  MQTT 发布
    → v2x/{scene_id}/vehicle/{vehicle_id}/status
    → v2x/{scene_id}/vehicle/{vehicle_id}/decision
```

#### 退化管理（FallbackManager）

| 模式          | 触发条件                           | 行为                            |
|---------------|-----------------------------------|---------------------------------|
| `cooperative` | 正常（200ms 内收到路侧消息）       | fusion_weight = 1.0，全速行驶   |
| `degraded`    | 连续 3 帧（>200ms × 3）未收到消息  | fusion_weight = 0.0，速度×0.5   |
| `recovering`  | 消息恢复后 3 秒渐变               | fusion_weight = elapsed/3.0s    |

---

### 4.3 云端孪生（Cloud Twin）

**入口**：`src/cloud_twin/cloud_agent.py`  
**API 模块**：`src/cloud_twin/api.py`  
**存储模块**：`src/cloud_twin/data_store.py`

#### CloudAgent

- 以 MQTT Client 身份订阅 **全部** `v2x/{scene_id}/*` 主题
- 每收到感知/决策消息：写入 SQLite + WebSocket 实时广播
- 事件检测：`TTC ≤ 2.0s` 且 `risk_level ∈ {DANGER, EMERGENCY}`，去抖动 5 秒后写入事件表

#### REST API

| 接口                                           | 说明                           |
|------------------------------------------------|--------------------------------|
| `GET /api/v1/health`                           | 健康检查 + WebSocket 连接数     |
| `GET /api/v1/frames/{frame_id}`                | 按帧 ID 查询感知/决策数据       |
| `GET /api/v1/replay/{scene_id}?start_ts&end_ts` | 时间范围回放                   |
| `GET /api/v1/events?scene_id&severity&limit`   | 事件列表（分页）               |
| `GET /api/v1/events/{event_id}`                | 单事件详情（含回放帧范围）       |
| `GET /api/v1/metrics`                          | 系统运行指标                   |
| `GET /api/v1/messages/recent?limit`            | 最近 MQTT 消息（内存缓冲）       |
| `WS /api/v1/realtime/ws`                       | WebSocket 实时推送              |

#### DataStore（SQLite WAL 模式）

```
frames 表
  frame_id INTEGER PRIMARY KEY
  timestamp INTEGER NOT NULL
  scene_id   TEXT NOT NULL
  node_id    TEXT
  perception_data  TEXT   ← JSON
  decision_data    TEXT   ← JSON
  vehicle_status   TEXT   ← JSON

events 表
  event_id          TEXT PRIMARY KEY
  timestamp         INTEGER
  event_type        TEXT
  severity          TEXT
  scene_id          TEXT
  min_ttc           REAL
  outcome           TEXT
  description       TEXT
  involved_objects  TEXT   ← JSON
  replay_start_frame INTEGER
  replay_end_frame   INTEGER
```

---

### 4.4 通信层（Communication）

**模块**：`src/communication/`

#### MQTT Client

- 基于 `paho-mqtt`，指数退避自动重连（1s → 30s）
- 订阅者回调模式，线程安全

#### Topic 约定

```
v2x/{scene_id}/roadside/{node_id}/perception   ← 路侧感知  QoS=1  10Hz
v2x/{scene_id}/roadside/{node_id}/heartbeat     ← 路侧心跳  QoS=0  2Hz
v2x/{scene_id}/vehicle/{vehicle_id}/status       ← 车端状态  QoS=1  10Hz
v2x/{scene_id}/vehicle/{vehicle_id}/decision     ← 车端决策  QoS=1  10Hz
v2x/{scene_id}/cloud/event                       ← 高危事件  QoS=1  事件触发时
v2x/{scene_id}/cloud/command                     ← 云端指令  QoS=1
```

---

### 4.5 前端

**技术栈**：React 18 + TypeScript + Vite 5 + Ant Design 5 + Zustand + React Router 6 + ECharts 5 + Three.js

#### 页面路由

| 路由       | 页面               | 功能                        |
|------------|--------------------|-----------------------------|
| `/`        | Dashboard（仪表盘） | KPI 卡 + 图表 + 消息流 + 事件列表 |
| `/monitor` | Monitor（实时监控） | WebSocket 消息 + Topic 管理 + 日志流 |
| `/replay`  | Replay（回放）      | 历史帧回放 + 事件详情        |
| `/evaluation` | Evaluation（评估） | 模型评测指标                 |
| `/settings` | Settings（设置）   | 主题 + 连接配置              |

#### 3D 场景

`frontend/src/features/three-scene/` — Three.js 路面/建筑/车辆/行人/预测轨迹/风险热力图层实时渲染

---

## 五、目录结构

```
E:\路云天瞳\luyuntiantong\
├── src\                              # 后端 Python 源码
│   ├── cloud_twin\                  # 云端孪生模块
│   │   ├── cloud_agent.py           # CloudAgent 主类 + run_api_server()
│   │   ├── api.py                   # FastAPI REST + WebSocket
│   │   └── data_store.py            # SQLite DataStore
│   ├── communication\               # 通信层
│   │   ├── mqtt_client.py           # MQTT 客户端封装
│   │   └── protocol.py              # 消息 Dataclass（DetectedObject / PerceptionMessage 等）
│   ├── roadside_perception\         # 路侧感知智能体
│   │   ├── roadside_agent.py        # 主入口
│   │   ├── detector.py              # YOLOv8 检测器
│   │   ├── occlusion_estimator.py   # 遮挡估计
│   │   ├── trajectory_predictor.py  # 轨迹预测
│   │   └── replay_engine.py         # 回放引擎（支持合成鬼探头场景）
│   ├── vehicle_decision\            # 车端决策智能体
│   │   ├── vehicle_agent.py         # 主入口
│   │   ├── risk_assessor.py         # TTC 风险评估
│   │   ├── brake_controller.py      # 分级制动
│   │   └── fallback_manager.py      # 通信降级/恢复状态机
│   └── utils\                       # 公共工具
│       ├── config.py                # YAML 配置加载
│       ├── logger.py                # 日志（控制台 + JSON 文件）
│       └── error_codes.py           # 错误码（E1xxx ~ E5xxx）
├── frontend\                        # 前端
│   ├── src\                         # React + Three.js 源码
│   ├── package.json
│   ├── vite.config.ts
│   └── dist\                        # 构建产物
├── configs\                         # YAML 配置（按 agent 拆分）
│   ├── cloud.yaml
│   ├── vehicle.yaml
│   ├── roadside.yaml
│   └── mqtt.yaml
├── deployment\                      # 部署文件
│   ├── Dockerfile
│   └── mosquitto.conf
├── data\                            # SQLite 数据库 + 回放数据
├── logs\                            # 运行日志
├── scripts\run_all.sh               # 一键启动脚本（Linux/macOS）
├── docker-compose.yml               # Docker Compose（Mosquitto + Cloud API）
├── requirements.txt                 # Python 依赖
├── docs\                            # 设计文档
│   ├── ARCHITECTURE.md
│   ├── MVP_REQUIREMENTS.md
│   ├── API_SPEC.md
│   ├── DATA_MODEL.md
│   ├── WEEKLY_PLAN.md
│   └── implementation_plan_v1.md
├── 启动.md                          # 启动文档（见下）
└── 项目介绍.md                      # ← 本文件
```

---

## 六、关键技术点

| 技术点              | 实现方式                                  |
|---------------------|-------------------------------------------|
| 事件去抖动          | CloudAgent 检测后等待 `cooldown_sec=5s`，防止假警 |
| 线程安全            | DataStore 用 `threading.Lock` + 上下文管理器 |
| MQTT / asyncio 桥接 | `asyncio.run_coroutine_threadsafe` 跨线程广播 |
| 退化恢复三态机      | cooperative → degraded → recovering → cooperative |
| 多 Python 版本兼容  | 同时保留 `.cpython-312` 和 `.cpython-313` 缓存 |
| 前端双模式          | Mock（本地定时器） / Real（WebSocket 连接后端） |

---

## 七、依赖与技术栈

### 后端（Python）

| 依赖              | 用途              |
|-------------------|-------------------|
| `numpy`           | 数值计算          |
| `pyyaml`          | 配置解析          |
| `paho-mqtt`       | MQTT 通信         |
| `ultralytics`     | YOLOv8 检测       |
| `torch/torchvision` | 深度学习推理    |
| `fastapi`         | REST API 框架     |
| `uvicorn`         | ASGI 服务器       |
| `python-json-logger` | 结构化日志      |

### 前端（Node.js）

| 依赖                  | 用途               |
|-----------------------|--------------------|
| `react` / `react-dom` | UI 框架            |
| `react-router-dom`    | 路由               |
| `antd`                | UI 组件库          |
| `three` + R3F + drei  | 3D 场景            |
| `echarts`             | 数据可视化图表     |
| `zustand`             | 状态管理           |
| `vite`                | 构建工具           |

---

*文档基于对 `src/`、`frontend/`、`configs/`、`deployment/`、`docs/` 的完整代码阅读生成，最后更新：2026-07-01。*
