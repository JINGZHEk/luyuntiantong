# 路云天瞳项目介绍

> 本文件用于说明作品的业务背景、功能边界和模块职责；比赛提交说明、启动命令和指标口径以根目录 [README.md](README.md) 为准。

## 1. 背景与问题

城市路口中存在大量由公交车、货车、建筑物、绿化和道路设施造成的遮挡区域。车辆驾驶员或车载摄像头在目标离开遮挡区之前无法获得完整视野，行人和非机动车也可能在没有明显预警的情况下进入车辆冲突路径。传统单车感知通常只能在目标已经接近车辆时报警，留给决策和制动的时间较短。

路云天瞳以“路侧看得更高、云端算得更远、车端执行更快”为设计主线：路侧节点补充全局视角，Cloud Agent 维护跨帧轨迹并预测未来 2 秒，车端根据预测结果给出 TTC、风险等级和制动建议，前端把数据链路和安全效果展示出来。

## 2. 作品目标

### 2.1 业务目标

- 提前发现被遮挡的行人、非机动车和车辆。
- 把多源感知结果统一到道路米制坐标，支持跨设备共享。
- 使用时序模型估计未来运动趋势，增加风险预警提前量。
- 对感知、预测、决策、告警和性能数据留痕，支持复盘和评估。

### 2.2 工程目标

- 线上无硬件也能启动完整业务演示。
- 硬件替换只影响采集与检测适配层，不改变 Cloud、数据库和前端协议。
- 模型加载失败、网络抖动、历史不足和 SQLite 堵塞时保持可解释降级。
- 训练、评估与在线推理采用一致的字段、采样率和窗口定义。

## 3. 端到端数据流

```text
视频/场景库
  -> FrameSource
  -> YOLO 检测（类别、bbox、confidence）
  -> DeepSORT 跟踪（track_id）
  -> 遮挡估计 + Homography（road_xy、coordinate_status）
  -> MQTT perception（10Hz）
  -> Cloud Agent（按 node_id + track_id 保存历史）
  -> TrajectoryDataset 规则/在线序列构建
  -> InferenceEngine（TorchScript、batch、warm-up）
  -> STGNN 未来 20 步轨迹
  -> TTC/风险/制动决策
  -> SQLite + WebSocket
  -> Web 实时大屏、事件回放、ADE/FDE 指标
```

时间统一为 Unix milliseconds；轨迹预测的相对未来时间 `t` 使用 seconds。路侧、Cloud、车端和前端通过 `scene_id`、`run_id`、`node_id`、`frame_id` 和 `timestamp` 对齐同一条时间线。

## 4. 模块职责

### 4.1 `src/roadside_perception`：路侧感知

- `frame_source.py`：读取视频、图片序列和回放帧，控制输入 FPS。
- `detector.py`：YOLO 或预计算标注适配器，输出标准检测结果。
- `tracker.py`：DeepSORT 适配器，维护稳定的整数 `track_id`。
- `occlusion_estimator.py`：根据场景遮挡区域和目标状态估计遮挡等级。
- `coordinate_mapper.py`：通过现场标定把 bbox 底边中心转换为 `road_xy`。
- `roadside_agent.py`：编排检测、跟踪、坐标、协议和 MQTT 发布。
- `stgnn_predictor.py`：提供预测适配与常速度 fallback；PC 模式默认由 Cloud 预测。

板端只发布结构化感知和心跳，不把原始视频通过 MQTT 感知 topic 传输。无效坐标会显式标记，不会填充零点。

### 4.2 `src/cloud_twin`：云端孪生与推理服务

- `cloud_agent.py`：订阅 MQTT，丰富感知消息，触发预测、事件和广播。
- `stgnn_service.py`：按 `(node_id, track_id)` 隔离轨迹历史，避免多路口 ID 冲突。
- `inference_engine.py`：模型单例、TorchScript 预加载、CUDA warm-up、有界微批、耗时和热更新。
- `prediction_writer.py`：后台队列写入预测、推理日志和异常，不阻塞推理回调。
- `data_store.py`：SQLite 表结构、场景运行、事件、回放和评估数据访问。
- `demo_engine.py`：无需 MQTT/YOLO/GPU 的场景库线上演示。
- `api.py`：健康、模型重载、场景、回放、指标、日志和 WebSocket API。

### 4.3 `src/vehicle_decision`：车端安全决策

车端融合自车状态与路侧目标，计算目标相对运动、TTC、风险等级、碰撞概率和制动建议。路侧消息过期或 Cloud 不可用时触发 fallback，避免网络故障直接变成无保护状态。

### 4.4 `src/dataset`：数据标准与训练评估

`trajectory_dataset.py` 负责 JSON/JSONL/SQLite 加载、质量清洗、10Hz 重采样和缺失处理。`stgnn_training_data.py` 将场景或真实轨迹转换为 20 帧输入、20 帧 GT 的监督样本；`train_stgnn.py` 完成增强、训练、验证、TorchScript 导出和实验落库；`stgnn_checkpoint_evaluator.py` 独立读取 checkpoint，输出 ADE/FDE/Miss Rate 和推理性能。

### 4.5 `src/scenario_library`：可复现场景库

场景库包含 16 个 GP/NM/IC 场景，每个场景定义参与者、关键帧、遮挡关系、自车状态和风险事件。编译器按 10Hz 展开关键帧，回放服务生成与真实 MQTT 相同结构的消息。场景 seed 固定，便于评审复现和自动化回归。

### 4.6 `frontend`：比赛展示前端

React/TypeScript 前端包含综合仪表盘、实时监控、历史回放、算法评估、参数设置、全屏演示和 Three.js“知路无界”大屏。实时页面订阅 perception、prediction、decision、event、vehicle_status 和 heartbeat；预测轨迹按置信度着色，性能面板读取真实健康接口。

## 5. 统一数据与模型约定

单个轨迹观测点：

```json
{
  "track_id": 7,
  "class": "car",
  "x": 12.4,
  "y": 3.1,
  "vx": 4.0,
  "vy": 0.0,
  "timestamp": 1710000000000,
  "confidence": 0.92
}
```

清洗规则为：低置信度丢弃；重复 `(track_id, timestamp)` 保留最高置信度；非有限或绝对值超过 200m 的坐标丢弃；不均匀数据重采样到 10Hz；连续缺失不超过 3 帧线性插值，超过 3 帧截断轨迹。监督窗口固定为历史 20 帧、未来 20 帧。

当前 TorchScript 输入形状为 `[batch, 20, 8]`，单帧特征为位置、框尺寸、速度、类别和遮挡信息；输出包含未来 `[batch, 20, 2]` 轨迹和遮挡分类 logits。当前训练样本以单目标序列为主；若比赛要求严格的多目标社会交互图，需要在同帧多节点样本和邻接特征基础上继续扩展，不能仅因模型名称包含 STGNN 就宣称已完成社会交互建模。

## 6. 数据库与可追溯性

| 表 | 作用 |
|---|---|
| `frames` | 感知、车辆状态和决策帧 |
| `events` | 风险事件、涉及目标和回放范围 |
| `predictions` | 预测点、速度、置信度和创建时间 |
| `inference_log` | batch size、推理耗时、目标数和时间戳 |
| `prediction_anomalies` | 单步位移过大等预测异常 |
| `experiments` | 训练参数、验证指标、模型路径和实验时间 |
| `scenario_runs` | 场景运行、seed、配置和回放元数据 |

所有预测和事件至少关联 `scene_id/run_id/track_id/timestamp`，可以从实时页面追溯到 API，再定位到 SQLite 记录和训练模型。

## 7. 完成范围与比赛边界

本次线上作品提交中，Web 界面、算法流程、场景回放、预测展示、性能监控、数据库记录和训练评估功能已完善，可直接通过一键 Demo 验证。线上平台只接受 Web 界面、算法流程及演示视频，无法让评审直接操作开发板、摄像头或无线链路，也无法在浏览器中测量硬件温度、功耗和物理吞吐。因此硬件部分以协议设计、迁移清单、架构说明和演示视频呈现；实物实时交互与物理性能不以 Web 仿真结果代替。

当前可作为工程回归的模型结果来自场景库，不能替代按路口隔离的真实道路或比赛盲测结果。硬件实测时应补充板端 FPS、YOLO/跟踪时延、ID switch、坐标有效率、MQTT 延迟、温度、功耗和持续运行稳定性。

## 8. 相关文档

- [比赛作品主 README](README.md)：完整作品介绍、启动、演示和提交说明。
- [启动与验证](启动.md)：环境、命令、测试和常见故障。
- [系统架构](docs/ARCHITECTURE.md)：运行模式、时序、背压和降级。
- [API 与消息契约](docs/API_SPEC.md)：MQTT、REST、WebSocket。
- [数据与模型](docs/DATA_MODEL.md)：字段、清洗、训练和指标口径。
- [STGNN 运行手册](docs/STGNN_RUNTIME.md)：样本、训练、推理和监控。
- [硬件迁移清单](docs/BOARD_MIGRATION_CHECKLIST.md)：Jetson/Atlas 接入和验收证据。
