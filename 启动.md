# 路云天瞳启动与验证

> 最后更新：2026-08-12

## 环境要求

- Python 3.11 或 3.12。
- Node.js 18+ 与 npm。
- 无硬件 Demo 不要求 MQTT Broker、YOLO 或 GPU。
- STGNN 训练/推理建议使用 CUDA PyTorch；不要使用 Python 3.9 环境。

安装后端依赖：

```powershell
python -m pip install -r requirements.txt
```

创建项目定义的算法环境并检查模型依赖：

```powershell
conda env create -f environment-algorithm.yml
conda activate v2x-ghost-algorithm
python scripts\verify_model_readiness.py --require-yolo
python scripts\verify_model_readiness.py --require-stgnn
```

`environment-algorithm.yml` 是可复现的基线；需要 GPU 时应确认该环境安装的是匹配本机驱动的 CUDA PyTorch，而不是 CPU build。

安装前端依赖：

```powershell
cd frontend
npm install
cd ..
```

## 一键 Demo

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_demo.ps1
```

常用参数：

```powershell
scripts\start_demo.ps1 -NoBrowser
scripts\start_demo.ps1 -BackendPort 8010 -FrontendPort 3010
scripts\start_demo.ps1 -Scenario heavy -Fps 10
scripts\start_demo.ps1 -SkipInstall
```

默认入口：

- 前端：`http://localhost:3000`
- 知路无界：`http://localhost:3000/zhiluwujie`
- Cloud API：`http://localhost:8000`
- 健康检查：`http://localhost:8000/health`

## 手动启动

后端：

```powershell
python -m uvicorn src.cloud_twin.api:app --host 127.0.0.1 --port 8000
```

前端：

```powershell
cd frontend
npm run dev
```

启动一个场景：

```powershell
Invoke-RestMethod -Method Post "http://127.0.0.1:8000/api/v1/demo/start?fps=10&scenario_id=GP-01"
```

查看状态或停止：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/v1/demo/status
Invoke-RestMethod -Method Post http://127.0.0.1:8000/api/v1/demo/stop
```

## PC → Cloud STGNN

配置文件：

- 路侧：`configs/roadside.pc.yaml`
- 云端：`configs/cloud.pc.yaml`
- 模型：`data/algorithm_validation_pipeline/models/occaware_stgnn.ts`

先检查配置，不启动进程：

```powershell
scripts\run_pc_cloud_demo.ps1 -Frames 60 -Fps 10 -DryRun
```

启动完整链路：

```powershell
scripts\run_pc_cloud_demo.ps1 -Frames 60 -Fps 10 -ApiPort 8000
```

该脚本默认寻找 `D:\Anaconda\envs\v2x-ghost-algorithm\python.exe`。如果实际 CUDA 环境路径不同，可直接用对应 `python.exe` 运行训练和评估脚本；完整 Cloud Agent 还需要安装 `requirements.txt` 中的 FastAPI、Paho MQTT 和 Uvicorn。

## STGNN 数据、训练和评估

从场景库生成 20/20 样本：

```powershell
python scripts\build_stgnn_training_data.py `
  --database data\v2x_cloud.db `
  --output data\stgnn_training `
  --input-steps 20 --future-steps 20 --fps 10
```

使用 CUDA Python 3.11 环境训练：

```powershell
& '<CUDA_PYTHON>\python.exe' scripts\train_stgnn.py `
  --samples data\stgnn_training\samples.jsonl `
  --output data\algorithm_validation_pipeline\models\occaware_stgnn.ts `
  --epochs 100 --batch-size 128 --hidden-dim 128 `
  --validate-every 10 --device auto `
  --experiments-db data\v2x_cloud.db
```

独立评估：

```powershell
& '<CUDA_PYTHON>\python.exe' scripts\evaluate_stgnn_checkpoint.py `
  --samples data\stgnn_training\samples.jsonl `
  --checkpoint data\algorithm_validation_pipeline\models\occaware_stgnn.ts `
  --output data\algorithm_validation_pipeline\stgnn_evaluation.json `
  --batch-size 128 --device auto
```

## 验证

核心后端测试：

```powershell
python -m unittest `
  tests.test_trajectory_dataset `
  tests.test_prediction_runtime `
  tests.test_cloud_stgnn_service `
  tests.test_stgnn_training_data `
  tests.test_stgnn_training_script `
  tests.test_stgnn_checkpoint_evaluation `
  tests.test_pc_cloud_pipeline
```

前端：

```powershell
cd frontend
npm run build
npm run test:unit
```

项目验证脚本：

```powershell
scripts\verify_all.ps1
```

## 常见问题

### 模型显示未加载

确认 `configs/cloud.pc.yaml` 的 `prediction.enabled=true`，模型文件存在，并用安装了 PyTorch 的 Python 启动 Cloud Agent。`POST /api/v1/model/reload` 可触发显式热更新。

### CUDA 可用但项目导入失败

PyTorch 环境还必须使用 Python 3.11+，并安装项目依赖。只复制 `torch` 文件夹不可行，应指定正确解释器或在该环境执行 `pip install -r requirements.txt`。

### 前端只有 `--` 指标

这表示健康接口未返回真实推理数据、GPU 不可用或尚未产生可对齐的预测真值，不会用 mock 数值冒充实时指标。

### 端口占用

一键脚本支持 `-BackendPort` 和 `-FrontendPort`。手动启动 Vite 可使用：

```powershell
npm run dev -- --port 5174
```

脚本只会复用通过 `V2X Cloud API health contract` 的后端，以及包含 `V2X frontend marker` 的前端。若端口被其他程序占用，会明确报告 `Port 8000 is already in use by another service` 或 `Port 5173 is already in use by another service`，此时应更换端口，不要继续连接错误服务。

## 相关文档

- [README.md](README.md)
- [项目介绍.md](项目介绍.md)
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)
- [docs/API_SPEC.md](docs/API_SPEC.md)
- [docs/DATA_MODEL.md](docs/DATA_MODEL.md)
- [docs/STGNN_RUNTIME.md](docs/STGNN_RUNTIME.md)
- [docs/BOARD_MIGRATION_CHECKLIST.md](docs/BOARD_MIGRATION_CHECKLIST.md)
- [docs/ONLINE_DEPLOYMENT.md](docs/ONLINE_DEPLOYMENT.md)
