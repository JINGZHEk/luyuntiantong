# V2X Ghost-Probe 平台启动指南

> 适用版本：v0.1  
> 当前阶段：仿真阶段 MVP，M0 闭环已通，M1 三端联动进行中  
> 最后更新：2026-07-06

---

## 1. 推荐启动方式：Windows 一键演示

当前最稳定的演示方式是 Cloud API 内置 demo loop。它不依赖 Mosquitto、YOLO、PyTorch 或实体硬件，可以直接跑通“鬼探头”端到端闭环：

```powershell
cd E:\路云天瞳\luyuntiantong
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_demo.ps1
```

脚本会自动完成：

| 步骤 | 内容 |
|------|------|
| 1 | 检查 Python 和 npm |
| 2 | 如缺少 `frontend\node_modules`，自动执行 `npm install` |
| 3 | 启动 FastAPI Cloud API，默认端口 `8000` |
| 4 | 调用 `/api/v1/demo/start?fps=10` 启动 demo loop |
| 5 | 启动 Vite 前端 dev server，默认端口 `3000` |
| 6 | 将 Cloud API 地址注入前端 dev server，并自动打开实时监控页 |

可选参数：

```powershell
# 不自动打开浏览器
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_demo.ps1 -NoBrowser

# 使用其他端口
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_demo.ps1 -BackendPort 8001 -FrontendPort 5173

# 跳过 npm install 检查
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_demo.ps1 -SkipInstall

# 修改 demo 帧率
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_demo.ps1 -Fps 15

# 指定鬼探头强度场景：light / moderate / heavy
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_demo.ps1 -Scenario heavy
```

---

## 2. 浏览器访问入口

启动成功后访问：

| 页面 | 地址 | 用途 |
|------|------|------|
| 实时监控 | http://localhost:3000/monitor | 查看实时感知、车辆状态、决策和 demo 控制 |
| 事件回放 | http://localhost:3000/replay | 查询 ghost_probe 事件并回放 3D 场景 |
| 模型评估 | http://localhost:3000/evaluation | 查看 demo runtime、mini split、ST-GNN checkpoint 或 YOLO Detection Offline 离线评估指标、基线和消融结果 |
| 系统设置 | http://localhost:3000/settings | 配置 Cloud API 地址、告警阈值、刷新频率，并与后端配置 API 同步 |
| API 文档 | http://localhost:8000/docs | 查看和调试 FastAPI 接口 |
| 健康检查 | http://localhost:8000/api/v1/health | 验证后端是否在线 |
| Demo 状态 | http://localhost:8000/api/v1/demo/status | 查看 demo loop 运行状态 |

如果启动脚本使用了自定义端口，请把上面的 `3000` 或 `8000` 替换成对应参数。`scripts\start_demo.ps1` 会在启动前端 dev server 时注入 `VITE_CLOUD_API_BASE_URL=http://localhost:<BackendPort>/api/v1`，因此新启动的前端会自动连接对应后端；系统会自动派生 WebSocket 地址 `ws://localhost:<BackendPort>/api/v1/realtime/ws`。如果前端进程本来已经运行，或者你选择手动启动前端，则可进入 `/settings` 页确认或修改“Cloud API 地址”。

`/settings` 页会在进入页面时尝试读取：

```text
GET http://localhost:8000/api/v1/config/scene_001
```

点击“保存到云端”后会写入：

```text
PUT http://localhost:8000/api/v1/config/scene_001
```

后端会把运行配置持久化到：

```text
data\runtime_config.json
```

该文件属于运行时生成物，可删除后恢复默认配置。

---

## 3. 手动启动：当前端到端 Demo

如果不使用一键脚本，可以分两个终端手动启动。

### 3.1 启动后端

```powershell
cd E:\路云天瞳\luyuntiantong
python -m uvicorn src.cloud_twin.api:app --host 0.0.0.0 --port 8000
```

新开一个 PowerShell，启动 demo loop：

```powershell
Invoke-WebRequest "http://localhost:8000/api/v1/demo/start?fps=10&scenario=moderate" -Method POST
```

常用 demo 控制接口：

```powershell
Invoke-WebRequest http://localhost:8000/api/v1/demo/status
Invoke-WebRequest "http://localhost:8000/api/v1/demo/step?scenario=light" -Method POST
Invoke-WebRequest http://localhost:8000/api/v1/demo/stop -Method POST
```

支持的 demo 场景强度：

| 场景 | 说明 |
|------|------|
| `light` | 低速、较晚进入高风险窗口，适合观察预警阶段 |
| `moderate` | 默认鬼探头强度，适合常规演示 |
| `heavy` | 高速、更早触发 DANGER/EMERGENCY，适合压力演示 |

### 3.2 启动前端

```powershell
cd E:\路云天瞳\luyuntiantong\frontend
npm install
npm run dev -- --host 0.0.0.0 --port 3000
```

---

## 4. 快速验证

后端和前端启动后，执行：

```powershell
Invoke-WebRequest http://localhost:8000/api/v1/health
Invoke-WebRequest http://localhost:8000/api/v1/config/scene_001
Invoke-WebRequest http://localhost:8000/api/v1/messages/recent?limit=5
Invoke-WebRequest http://localhost:8000/api/v1/events?limit=5
```

也可以生成事件后验证回放帧：

```powershell
Start-Sleep -Seconds 7
$eventsJson = Invoke-WebRequest http://localhost:8000/api/v1/events?limit=1 -UseBasicParsing | Select-Object -ExpandProperty Content
$eventId = ($eventsJson | ConvertFrom-Json).events[0].event_id
$detail = Invoke-WebRequest "http://localhost:8000/api/v1/events/$eventId" -UseBasicParsing | Select-Object -ExpandProperty Content
($detail | ConvertFrom-Json).replay_frames.Count
```

预期结果：

- `/monitor` 页面能看到 perception、vehicle_status、decision、event 持续更新。
- 连接面板显示 live Cloud API WebSocket 数据源；后端不可用时才回退到 mock。
- `/settings` 页面能从 Cloud API 加载配置，保存后 `data\runtime_config.json` 会更新。
- `/replay` 页面能加载真实 `ghost_probe` 事件。
- 事件详情返回 `replay_frames`，3D 场景能回放车辆、行人、停放车辆、TTC、风险分和制动状态。

### 4.1 本地完整验证

需要一次性跑完当前基础验证时，执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify_all.ps1
```

该脚本会依次运行：

| 步骤 | 命令范围 |
|------|----------|
| 后端单元测试 | 覆盖 MQTT demo、嵌入式 Broker、demo engine、数据集 manifest、mini split evaluation、DAIR 数据发现、YOLO 图片推理、YOLO detection 批量评估、ST-GNN 与算法流水线 |
| 外部环境预检 | `scripts\verify_external_readiness.py` 汇总真实 DAIR-V2X、Docker、外部 MQTT Broker、YOLO/ST-GNN 算法环境 readiness |
| 内存三端验证 | `scripts\verify_inmemory_mqtt_demo.py --scenario heavy --frames 80 --verify-fallback` |
| M2 样例评估验证 | `scripts\verify_m2_demo_sample.py --frames 60 --horizon 30` |
| DAIR 数据集发现脚本测试 | `tests.test_dair_dataset_discovery` |
| 模型环境诊断 | `scripts\verify_model_readiness.py` |
| YOLO 图片推理脚本测试 | `tests.test_yolo_image_inference` |
| 算法流水线 dry-run | `scripts\verify_algorithm_pipeline.py` |
| 前端单测 | `npm run test:unit` |
| 前端 lint | `npm run lint` |
| 前端构建 | `npm run build` |

可选跳过部分检查：

```powershell
# 只跑后端和内存三端
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify_all.ps1 -SkipFrontend

# 跳过前端生产构建，只跑前端单测和 lint
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify_all.ps1 -SkipBuild

# 跳过 M2 DAIR 风格样例评估验证
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify_all.ps1 -SkipM2DemoSample

# 跳过 YOLO/ST-GNN 模型环境 readiness 诊断
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify_all.ps1 -SkipModelReadiness
```

仓库也包含 `.github\workflows\ci.yml` 基础 CI，用于在 GitHub Actions 中自动运行后端测试、brokerless MQTT 验证、M2 DAIR 风格样例评估验证、模型环境 readiness 诊断和前端 test/lint/build。
CI 的 backend job 还会在 Ubuntu runner 中安装并启动 Mosquitto，然后运行：

```powershell
python scripts\verify_mqtt_broker_demo.py --frames 80 --fps 10 --verify-fallback
```

这一步用于验证 Roadside Agent -> 外部 Mosquitto Broker -> Vehicle Agent -> Mosquitto Broker -> Cloud Agent 的真实 Broker 链路；它不依赖当前 Windows 本机已安装 Mosquitto 或 Docker。

需要快速确认当前机器距离完整外部验收还缺哪些环境时，运行：

```powershell
python scripts\verify_external_readiness.py
```

默认模式只输出 JSON 摘要，不会因为缺少真实 DAIR-V2X、Docker、外部 MQTT Broker 或算法环境而失败；适合本地日常检查和 CI 留痕。需要把这些外部条件作为强制验收项时，使用严格模式：

```powershell
python scripts\verify_external_readiness.py --search-root E:\路云天瞳 --require-real-dair --require-docker --require-broker --require-algorithm
```

其中 `--require-real-dair` 会要求发现真实 DAIR-V2X 数据目录，`--require-docker` 会要求 Docker/Compose 可用且 Compose 配置合同通过，`--require-broker` 会要求指定 MQTT Broker 端口可连接，`--require-algorithm` 会要求 YOLO 与 ST-GNN 依赖均可用。

仓库还包含 `.github\workflows\algorithm.yml`，用于手动触发或每周调度真实算法环境验证。该 workflow 会用 `environment-algorithm.yml` 创建 Conda 环境，并运行：

```powershell
python scripts\verify_model_readiness.py --require-yolo --require-stgnn
python scripts\verify_yolo_image_inference.py --min-detections 1
python scripts\verify_algorithm_pipeline.py --work-dir data\algorithm_validation_ci --frames 32 --horizon 5 --real-stgnn --python python --epochs 8 --batch-size 4 --hidden-dim 32
```

该 workflow 用于验证真实 YOLO 推理路径和 ST-GNN 小样本训练/checkpoint 评估链路，不等同于真实 DAIR-V2X 指标达标报告。

---

## 5. M1 路线：MQTT 三端联动启动

下面是 M1 阶段目标链路，用于 Roadside Agent、Vehicle Agent、Cloud Agent 的真实 MQTT 联动。当前项目已具备相关代码入口，但完整 Windows 一键联动仍在 GOAL.md 的 P1/P0 后续项中。

### 5.0 Windows 三端联动脚本

```powershell
cd E:\路云天瞳\luyuntiantong
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_mqtt_demo.ps1
```

脚本会检查 `1883` 端口是否已有 MQTT Broker；如果没有，会尝试启动本地 `mosquitto` 或 Docker Compose 的 `mosquitto` 服务。随后依次启动：

| 进程 | 入口 |
|------|------|
| Cloud Agent + API | `python -m src.cloud_twin.cloud_agent --api-port 8000` |
| Vehicle Agent | `python -m src.vehicle_decision.vehicle_agent` |
| Roadside Replay Engine | `python -m src.roadside_perception.replay_engine --fps 10` |

注意：前端默认连接 `http://localhost:8000/api/v1`，因此三端联动脚本默认也使用 `8000`。如果该端口已经被 M0 demo loop 占用，可以先停止原后端进程再启动 M1 脚本；也可以用 `-ApiPort` 指定其他端口，并在前端 `/settings` 页同步修改“Cloud API 地址”。

如果本机暂时没有 Mosquitto 或 Docker，可以先运行进程内验证模式：

```powershell
python scripts\verify_inmemory_mqtt_demo.py --scenario heavy --frames 80 --verify-fallback
```

该模式不打开网络端口，不连接真实 Broker，但会通过内存 MQTT 总线跑通 Roadside -> Vehicle -> Cloud 的 topic 流、云端落库、同帧数据合并和 ghost_probe 事件生成，用于验证三端消息逻辑。加上 `--verify-fallback` 后，还会模拟路侧消息中断和恢复，验证车端状态从 `cooperative` 进入 `degraded`，再进入 `recovering`。
验证摘要会同时输出 `avg_e2e_latency_ms`、`max_e2e_latency_ms`、`e2e_latency_sample_count`、`latency_target_ms=100.0` 和 `latency_target_passed`，用于持续检查 M1 “端到端延迟 < 100 ms”的轻量验收门槛。

如果本机已经有 Mosquitto 或 Docker Broker 在 `1883` 监听，可以运行真实网络 Broker 验收：

```powershell
python scripts\verify_mqtt_broker_demo.py --frames 80 --fps 10 --verify-fallback
```

该脚本会使用真实 `paho-mqtt` 连接跑 Roadside Agent -> MQTT Broker -> Vehicle Agent -> MQTT Broker -> Cloud Agent，检查云端是否形成完整合并帧、是否生成 `ghost_probe` 事件，并可验证车端 `cooperative -> degraded -> recovering`。若 Broker 不存在，脚本会输出结构化诊断：
当 Broker 可用且三端链路跑通时，结果也会包含同一组端到端延迟指标；如果 `latency_target_passed=false`，结果校验会按延迟目标失败处理。

```json
{
  "broker_available": false,
  "broker": "127.0.0.1:1883",
  "complete_frames": 0,
  "event_count": 0
}
```

如果本机没有 Mosquitto/Docker，但已经创建算法环境并可安装 Python 包，可以使用嵌入式 `amqtt` 启动一个真实 TCP MQTT Broker 做强校验：

```powershell
& 'D:\Anaconda\envs\v2x-ghost-algorithm\python.exe' -m pip install amqtt
& 'D:\Anaconda\envs\v2x-ghost-algorithm\python.exe' scripts\verify_embedded_mqtt_broker_demo.py --frames 80 --fps 10 --verify-fallback --min-complete-frames 20
```

该模式会在本机随机空闲端口启动 Broker，并验证 Roadside Agent -> TCP MQTT Broker -> Vehicle Agent -> TCP MQTT Broker -> Cloud Agent 的真实网络闭环。当前机器已通过一次验收：`complete_frames=80`、`event_count=1`、`fallback_verified=true`。注意它不等同于 Mosquitto/Docker 外部 Broker 验收，后者仍需在具备对应环境后运行 `scripts\verify_mqtt_broker_demo.py`。

### 5.1 启动 MQTT Broker

Windows 本地 Mosquitto：

```powershell
mosquitto -d -c deployment\mosquitto.conf
```

或 Docker：

```powershell
docker run -d -p 1883:1883 -p 9001:9001 eclipse-mosquitto:2
```

### 5.2 启动三端智能体

```powershell
# 终端 1：Cloud Agent
python -m src.cloud_twin.cloud_agent

# 终端 2：Vehicle Agent
python -m src.vehicle_decision.vehicle_agent

# 终端 3：Roadside Replay Engine
python -m src.roadside_perception.replay_engine
```

辅助监控 MQTT：

```powershell
python scripts\mqtt_monitor.py
```

---

## 6. Docker Compose

当前 `docker-compose.yml` 已覆盖以下容器：

| 服务 | 端口 | 说明 |
|------|------|------|
| `mosquitto` | `1883` / `9001` | MQTT Broker / WebSocket Broker |
| `cloud-api` | `8000` | FastAPI Cloud API，适合 M0 demo loop 和前端访问 |
| `frontend` | `3000` -> 容器 `80` | Nginx 托管的前端静态站点 |
| `cloud-agent` | `8001` | `mqtt-demo` profile 下的 CloudAgent + API |
| `vehicle-agent` | 无外部端口 | `mqtt-demo` profile 下的 VehicleAgent |
| `replay-engine` | 无外部端口 | `mqtt-demo` profile 下的 Roadside replay publisher |

在没有 Docker 命令的机器上，也可以先验证 Compose 配置合同：

```powershell
python scripts\verify_docker_compose_config.py
```

该脚本不启动容器，会检查服务数量、Dockerfile 路径、端口映射、`mqtt-demo` profile、`depends_on` 关系、Mosquitto 配置挂载，以及三端容器的 `MQTT_HOST=mosquitto` / `MQTT_PORT=1883` 环境变量。本地 `scripts\verify_all.ps1` 和 GitHub Actions 基础 CI 已包含该检查。

基础前后端栈：

```powershell
docker-compose up -d
docker-compose logs -f
docker-compose down
```

启动后访问：

```text
http://localhost:3000
http://localhost:8000/docs
```

如果要启动真实 MQTT 三端容器链路，使用 `mqtt-demo` profile：

```powershell
docker compose --profile mqtt-demo up --build
```

或旧版 Compose：

```powershell
docker-compose --profile mqtt-demo up --build
```

容器内三端进程通过环境变量连接 Broker：

```text
MQTT_HOST=mosquitto
MQTT_PORT=1883
```

注意：当前本机环境如果没有 Docker 命令，无法执行上述容器实跑；可先使用第 5 节的 `verify_inmemory_mqtt_demo.py` 和 `verify_mqtt_broker_demo.py` 进行非容器验证。

---

## 7. M2 数据集入口：DAIR-V2X mini split

先确认本机是否已经有真实 DAIR-V2X 风格数据目录：

```powershell
cd E:\路云天瞳\luyuntiantong
python scripts\verify_dair_dataset.py --search-root E:\路云天瞳
```

脚本会扫描 `infrastructure-side\image` 和 `infrastructure-side\label` 结构，并输出候选目录、图片数、标注数、是否为项目生成的 `demo_dair_sample`。如果要把真实数据作为强制验收项，使用：

```powershell
python scripts\verify_dair_dataset.py --search-root E:\路云天瞳 --require-real
```

当前机器的诊断结果是：只发现项目生成的 demo sample，`real_candidate_count=0`，因此严格模式会失败。把真实 DAIR-V2X 解压到本机后，也可以直接指定：

```powershell
python scripts\verify_dair_dataset.py --dair-root E:\datasets\DAIR-V2X --require-real
```

如果当前机器还没有真实 DAIR-V2X 数据集，可以先生成 DAIR 风格的鬼探头样例数据，打通 build -> replay clip -> evaluation 的完整链路：

```powershell
cd E:\路云天瞳\luyuntiantong
python scripts\build_dair_mini_split.py --demo-sample --output data\mini_split --max-frames 60 --scene-id demo_dair_001
python scripts\evaluate_mini_split.py --clip data\mini_split\replay\clip_001.json --output data\mini_split\evaluation.json --horizon 30
```

该样例只用于工程链路验证和前端评估页演示，不等同于真实 DAIR-V2X 实验结果。生成的 `evaluation.json` 会被后端 `/api/v1/evaluation` 优先读取。

也可以用专门的验证脚本一次性完成上述链路，并输出 JSON 摘要：

```powershell
python scripts\verify_m2_demo_sample.py --frames 60 --horizon 30
```

该脚本还会从 replay clip 生成 OccAware-STGNN 监督样本，写出 `stgnn_evaluation.json` 的 checkpoint 评估 dry-run 报告，并生成 `yolo_detection.json` 的 YOLO detection dry-run 报告。dry-run 只验证样本、checkpoint 路径、检测报告和前端报告选择链路，不会导入 torch，也不会声明真实模型指标。

如果本机已经下载 DAIR-V2X 数据集，可先生成项目内部 mini split 清单和 replay clip：

```powershell
cd E:\路云天瞳\luyuntiantong
python scripts\build_dair_mini_split.py --dair-root E:\datasets\DAIR-V2X --output data\mini_split --max-frames 100
```

脚本会扫描 DAIR 风格目录中的 `infrastructure-side\image` 和 `infrastructure-side\label`，生成：

| 文件 | 用途 |
|------|------|
| `data\mini_split\manifest.json` | 数据集帧清单、图片路径、标注路径和帧数量 |
| `data\mini_split\replay\clip_001.json` | `ReplayEngine` 可读取的项目内部帧格式 |

继续生成离线评估报告：

```powershell
python scripts\evaluate_mini_split.py --clip data\mini_split\replay\clip_001.json --output data\mini_split\evaluation.json --horizon 30
```

如果 `data\mini_split\replay` 下有多个 `clip_*.json`，可以生成目录级聚合报告：

```powershell
python scripts\evaluate_mini_split.py --replay-dir data\mini_split\replay --output data\mini_split\evaluation.json --horizon 30
```

该报告会输出 ADE、FDE、Occ-ADE、Occ-Acc、FPS、E2E-Lat、Lead-Time 等指标，并在 `targetStatus` 中按 `GOAL.md` 阈值标记 `pass/fail/unknown`。其中 Lead-Time 表示首次遮挡帧到目标暴露帧的提前预警秒数，目标为 `>= 1.5s`；目录级报告还会包含 `clip_count` 和每个 clip 的子报告摘要。后端 `/api/v1/evaluation` 默认读取 `data\mini_split\evaluation.json`，前端评估页会通过 `/api/v1/evaluation/reports` 列出可用报告，并可切换到 `mini_split`、`stgnn_checkpoint` 或 `yolo_detection` 报告；旧报告缺少 `targetStatus` 时会自动回填。如需切换整个评估报告目录，可先设置环境变量：

```powershell
$env:V2X_EVALUATION_DIR="E:\path\to\reports"
```

如需用单个文件覆盖默认报告，可设置：

```powershell
$env:V2X_EVALUATION_REPORT="E:\path\to\evaluation.json"
```

当前该入口用于打通 M2 数据边界和轻量轨迹预测 baseline；真实 DAIR-V2X 下载、YOLO/ST-GNN 模型训练和指标达标仍属于后续目标。

如果要把 M2 数据生成、ST-GNN 样本导出、训练脚本和 checkpoint 评估整合成一个验收入口，可运行：

```powershell
python scripts\verify_algorithm_pipeline.py --work-dir data\algorithm_validation --frames 60 --horizon 30
```

默认模式不导入 torch，只生成 dry-run 摘要。完成 `environment-algorithm.yml` 环境创建后，可以用真实算法环境执行小样本训练和 checkpoint 评估：

```powershell
python scripts\verify_algorithm_pipeline.py --work-dir data\algorithm_validation_pipeline --frames 32 --horizon 5 --real-stgnn --python D:\Anaconda\envs\v2x-ghost-algorithm\python.exe --epochs 8 --batch-size 4 --hidden-dim 32
```

该命令会先强制检查 YOLO/ST-GNN 依赖，再训练一个小样本 TorchScript checkpoint，并输出 `stgnn_checkpoint_offline` 评估报告。该报告用于验证真实算法链路，不代表最终 DAIR-V2X 达标结果。

生成 OccAware-STGNN 监督训练样本：

```powershell
python scripts\build_stgnn_training_data.py --clip data\mini_split\replay\clip_001.json --output data\stgnn_training --history-length 8 --predict-steps 30 --target-class person
```

输出文件：

| 文件 | 用途 |
|------|------|
| `data\stgnn_training\samples.jsonl` | 每行一个训练样本，包含历史节点特征、未来轨迹和遮挡标签 |
| `data\stgnn_training\manifest.json` | 样本数量、遮挡样本数量、窗口参数和源 clip |

该训练样本导出不依赖 torch，可在当前轻量环境运行；真正训练模型和导出训练后 checkpoint 仍建议使用 `environment-algorithm.yml`。

校验训练样本和训练配置，不导入 torch：

```powershell
python scripts\train_stgnn.py --samples data\stgnn_training\samples.jsonl --output models\occaware_stgnn.ts --epochs 5 --batch-size 16 --dry-run
```

在 Python 3.10/3.11 算法环境中执行真实训练并导出 checkpoint：

```powershell
python scripts\train_stgnn.py --samples data\stgnn_training\samples.jsonl --output models\occaware_stgnn.ts --epochs 50 --batch-size 32
```

校验 checkpoint 评估报告结构，不导入 torch：

```powershell
python scripts\evaluate_stgnn_checkpoint.py --samples data\stgnn_training\samples.jsonl --checkpoint models\occaware_stgnn.ts --output data\mini_split\stgnn_evaluation.json --dry-run
```

在算法环境中加载训练后的 TorchScript checkpoint 并计算指标：

```powershell
python scripts\evaluate_stgnn_checkpoint.py --samples data\stgnn_training\samples.jsonl --checkpoint models\occaware_stgnn.ts --output data\mini_split\stgnn_evaluation.json --batch-size 32
```

生成 `data\mini_split\stgnn_evaluation.json` 后，前端 `/evaluation` 页顶部的报告选择器会出现 `OccAware-STGNN Checkpoint` 选项；dry-run 报告会显示待测状态，真实算法环境生成的报告才可作为 ST-GNN 指标结果。

### 7.1 路侧检测模式

默认 `configs\roadside.yaml` 使用标注直读模式：

```yaml
detection:
  mode: annotations
```

该模式会直接读取 replay clip 中的 `annotations`，适合 M0/M1 demo、CI、Docker 轻量容器和 DAIR-V2X 标注回放，不需要安装 `ultralytics` 或加载 YOLO 权重。

如果要切换到真实 YOLO 推理模式，将配置改为：

```yaml
detection:
  mode: yolo
  model: yolov8n
```

该模式下，如果 replay frame 同时包含 `image` 和 `annotations`，Roadside Agent 会优先对 `image` 执行 YOLO 推理；只有缺少图片时才会回落到 annotations，避免真实模型验收被标注直读路径绕过。

同时准备独立的 Python 3.11 算法环境。仓库已提供 `environment-algorithm.yml`，默认使用 CPU 基线，包含 PyTorch、torchvision、ultralytics、torch-geometric 和项目运行依赖：

```powershell
conda env create -f environment-algorithm.yml
conda activate v2x-ghost-algorithm
```

当前机器已按该文件创建 `v2x-ghost-algorithm` 环境，并通过 `--require-yolo --require-stgnn` 强制 readiness 验收：Python 3.11.15、ultralytics 8.4.89、torch 2.5.1、torchvision 0.20.1、torch_geometric 2.8.0 可用。

如果使用 GPU/CUDA 机器，可按本机驱动版本调整 `environment-algorithm.yml` 中的 PyTorch channel / CUDA 包配置，再创建环境。

可先运行模型环境 readiness 诊断：

```powershell
python scripts\verify_model_readiness.py
```

如果要把 YOLO 或 ST-GNN 依赖作为强制验收项，在算法环境中运行：

```powershell
python scripts\verify_model_readiness.py --require-yolo
python scripts\verify_model_readiness.py --require-stgnn
```

完成 readiness 后，可以用真实图片跑一次 YOLOv8n 推理烟测：

```powershell
& 'D:\Anaconda\envs\v2x-ghost-algorithm\python.exe' scripts\verify_yolo_image_inference.py --min-detections 1
```

默认输入是 ultralytics 包内的 `bus.jpg` 真实照片，默认模型是 `yolov8n`。脚本会通过项目 `Detector(mode="yolo")` 路径执行推理，并输出 `image`、`model_loaded`、`detection_count`、`classes` 和检测框列表。当前机器已验收通过：`detection_count=4`，类别包含 `bus` 和 `person`。下载的权重会缓存到：

```text
data\model_cache\yolov8n.pt
```

该目录属于运行生成物，不需要提交。此烟测证明 YOLO 图片推理路径可运行，但不等同于 DAIR-V2X 真实数据集批量检测指标。

### 7.2 YOLO 检测离线批量评估

如果已经通过 `build_dair_mini_split.py` 生成了 `data\mini_split\manifest.json`，可以先用 dry-run 验证检测评估报告结构和前端展示链路：

```powershell
python scripts\evaluate_yolo_detection.py --manifest data\mini_split\manifest.json --output data\mini_split\yolo_detection.json --dry-run --max-frames 20
```

dry-run 会把标注当作完美检测结果，适合确认 manifest、标注解析、IoU 匹配、报告 JSON 和 `/evaluation` 页面选择器是否打通；它不是真实 YOLO 指标。

在 Python 3.11 算法环境中运行真实 YOLO 批量检测：

```powershell
& 'D:\Anaconda\envs\v2x-ghost-algorithm\python.exe' scripts\evaluate_yolo_detection.py --manifest data\mini_split\manifest.json --output data\mini_split\yolo_detection.json --max-frames 20
```

报告默认写入：

```text
data\mini_split\yolo_detection.json
```

后端 `/api/v1/evaluation/reports` 会列出 `YOLO Detection Offline`；前端 `/evaluation` 页顶部的报告选择器可以切换查看该报告。报告包含总体 precision、recall、F1、平均延迟、FPS，以及 TP/FP/FN、per-class 和逐帧检测摘要。

当前机器尚未发现真实 DAIR-V2X 数据，`verify_dair_dataset.py --require-real` 的 `real_candidate_count=0`，因此真实 DAIR-V2X 批量检测指标仍需在数据落盘后执行上述真实 YOLO 命令产出。

### 7.3 轨迹预测 backend

默认 `configs\roadside.yaml` 使用常速度轨迹预测 baseline：

```yaml
prediction:
  backend: constant_velocity
```

如果已有 OccAware-STGNN TorchScript checkpoint，可切换为：

```yaml
prediction:
  backend: stgnn
  model_path: models\occaware_stgnn.ts
```

当前 `stgnn` backend 已能构建 8 维节点特征并尝试加载 TorchScript 权重；如果未配置权重、权重不存在或 torch 不可用，会在日志和 `backend_status` 中标记原因，并回落到常速度预测。该 fallback 只用于工程链路打通，不等同于真实 ST-GNN 实验结果。

查看当前模型规格：

```powershell
python scripts\export_stgnn_checkpoint.py --describe
```

在 Python 3.10/3.11 算法环境中，可以导出一个随机初始化的 TorchScript checkpoint 做链路集成测试：

```powershell
python scripts\export_stgnn_checkpoint.py --output models\occaware_stgnn.ts --history-length 8 --predict-steps 30
```

注意：上述 checkpoint 未训练，只用于验证 Roadside Agent 能加载和调用 ST-GNN 模型；真实指标必须使用 DAIR-V2X 训练后的权重重新评估。

---

## 8. 前置环境

| 软件 | 建议版本 | 用途 |
|------|----------|------|
| Python | 3.10-3.13 | FastAPI、demo loop、智能体 |
| Node.js | 18+ | React/Vite 前端 |
| npm | 随 Node.js 安装 | 前端依赖管理 |
| Mosquitto | 可选 | M1 MQTT 三端联动 |
| Docker | 可选 | Broker / Cloud API 容器化 |

注意：当前 Python 3.13 环境下 `ultralytics` / `torch` 可能没有完整可用 wheel。需要 YOLOv8 或 ST-GNN 时，建议使用 Conda Python 3.10/3.11 单独固定算法环境。M0 demo loop 不依赖这些库。

---

## 9. 常见问题

### PowerShell 禁止执行脚本

使用单次绕过策略：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_demo.ps1
```

### 端口被占用

检查端口：

```powershell
netstat -ano | Select-String "8000|3000|5173"
```

换端口启动：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\start_demo.ps1 -BackendPort 8001 -FrontendPort 5173
```

如果使用 `scripts\start_demo.ps1` 启动，脚本会把 Cloud API 地址自动传给前端。若前端进程已经提前运行，或你手动启动前端，则打开 `http://localhost:5173/settings`，将 Cloud API 地址设置为：

```text
http://localhost:8001/api/v1
```

### 前端依赖缺失

```powershell
cd frontend
npm install
```

### 后端缺少依赖

```powershell
pip install -r requirements.txt
```

### 查看启动日志

一键脚本日志位于：

```text
logs\demo_backend.log
logs\demo_backend.err.log
logs\demo_frontend.log
logs\demo_frontend.err.log
```

---

## 10. 相关文档

| 文档 | 说明 |
|------|------|
| `GOAL.md` | 项目目标总纲、阶段路线、验收标准 |
| `项目介绍.md` | 项目背景、架构、模块与目录说明 |
| `docs/END_TO_END_DEMO.md` | 当前真实端到端 demo 闭环说明 |
| `docs/API_SPEC.md` | MQTT Topic、REST API、WebSocket 接口 |
| `docs/ARCHITECTURE.md` | 路侧、车端、云端总体架构 |
